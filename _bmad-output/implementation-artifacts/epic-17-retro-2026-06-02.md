# Epic-17 Retrospective — 2026-06-02

**Epic:** E17 Monitoring, Logging, and Health Checks (REQ-088)
**Mode:** Single autonomous-mode session per user directive: "implementiere das ganze epic 17 mit den stories. höre erst auf wenn alle stories geamcht sind und führe danach das retro aus. nicht stoppen bis es durch ist. berücksichtige dabei das es sich nicht um ein mvp handelt." (2026-06-02)
**Outcome:** **Closed.** 3 stories shipped + 3 docs/14 sections + 25 new tests + 14 boundary-review patches applied + 14 deferred-work entries logged + retro complete.

## What we built

| Story | Surface | New files | Tests added | docs/14 section | LOC delta (rough) |
|---|---|---|---|---|---|
| E17-S1 | Console-only Serilog overlay + bootstrap + Dockerfile audit | `ConsoleOnlySerilogConfigurationTests.cs` + `BootstrapSerilogConfigurationTests.cs` | 8 | §25 (~110 lines, 6 subsections) | ~370 lines |
| E17-S2 | CorrelationId middleware behavior + pipeline regression + LogContext continuity/isolation + log-level config + A31 invariant | `CorrelationIdMiddlewareTests.cs` + `RequestLoggingPipelineTests.cs` | 12 | §26 (~140 lines, 8 subsections) | ~545 lines |
| E17-S4 | `/health/ready` code-audit + UptimeRobot/BetterStack/Kuma runbook + A31 path parity | `HealthReadyEndpointTests.cs` | 5 | §27 (~290 lines, 9 subsections) | ~465 lines |
| **TOTAL** | | 5 NEW test files | **25 new tests** | 3 new sections (~540 lines) | **~1380 lines** |

**Production code change:** 0 lines. Entire epic was verification + documentation (each story's refresh found the AC literal text was already met, having been authored under REQ-054 or earlier Beta-prep stories).

## Quality gates

| Gate | Result |
|---|---|
| `dotnet build IabConnect.sln -warnaserror` | 0 warnings, 0 errors |
| Full backend test suite (post-patches) | **2075 / 2075 passed** (Application 1442 + Api 219 + Infrastructure 414) |
| Epic-boundary regression delta | +25 tests vs. pre-epic baseline (2050 → 2075) |
| Targeted E17 filter | 25 / 25 passed |
| docs/14 line count delta | +540 lines (sections 25, 26, 27) |

## Decisions resolved per A41 autonomous-mode escape + A43 (a)/(b)/(c) Debug Log

Seven DEC-Needed surfaces across the three stories, all resolved cleanly:

| Story | DEC | Chosen | One-line rationale |
|---|---|---|---|
| E17-S1 | DEC-1 (test mechanism) | A | Direct artifact-read per A51 + A49 sidestep — mirrors E14-S2 SecurityHeadersTests precedent |
| E17-S1 | DEC-2 (Production overlay) | B | Skip + document inheritance; let E19 introduce Production overlay with intentional posture |
| E17-S2 | DEC-1 (continuity test) | A | TestCorrelator already in project (E14-S5); mirrors SensitiveDataDestructuringPolicyTests precedent |
| E17-S2 | DEC-2 (isolation test) | A | Parallel Task.Run + LogContext.PushProperty; idiomatic AsyncLocal demonstration |
| E17-S2 | DEC-3 (scope expansion) | A | Minimum literal scope; defer request-log enrichment to a future story |
| E17-S4 | DEC-1 (monitor service) | C | UptimeRobot + BetterStack + Uptime Kuma side-by-side, OSS-fork-friendly per post-MVP posture |
| E17-S4 | DEC-2 (cadence/alert rule) | A | Relax AC-2 to align with ADR-017's free-tier reality; latency floor documented transparently |
| E17-S4 | DEC-3 (fire drill method) | A | Railway dashboard Stop > Restart; cleanest, repeatable; alternatives documented as fallback |

All (a)/(b)/(c) Debug Log entries present in each story's Dev Agent Record per A43 — verified by Acceptance Auditor.

## A47 escape application

**14 `[!]` live-walkthrough items** uniformly deferred-pending-beta-green per A47 to the unified Wave-8/9 walkthrough:

- E17-S1: 3 items (Q1 Railway logs visible + Q2 no `IOException` + Q3 `/app/logs` absent)
- E17-S2: 3 items (Q1 curl with header echoes + Q2 Railway log filter + Q3 no-header GUID generation)
- E17-S4: 8 items (Q1-Q3 UptimeRobot setup + Q4-Q5 BetterStack setup + Q6 dashboard URL + Q7 fire-drill execution + Q8 latency comparison)

Each story's Completion Notes contains the Q-list as the operator-facing artifact.

## Epic-boundary review (3-layer adversarial)

Per `feedback_bmad_workflow` hybrid CR+ER policy. Three subagents fanned out: Blind Hunter + Edge Case Hunter + Acceptance Auditor. **39 unique findings** consolidated → **14 patches APPLIED** in-session, **14 deferred** to deferred-work.md, **11 dismissed** (cosmetic / theoretical / scope-out).

Full breakdown: [`epic-17-boundary-review-2026-06-02.md`](epic-17-boundary-review-2026-06-02.md).

### Highest-impact patches (re-summary)

- **P5/E2** AC-5 test's A36 InMemoryCollection-guard was theatre (empty dict, no AddEnvironmentVariables chain). Patched to populate dict + chain AddEnvironmentVariables.
- **E1** AC-5 test missed the parallel `Logging:LogLevel:*` tree (Microsoft.Extensions.Logging adapter surface). Added 3 assertions.
- **E3** HealthReady auth test used fragile `IndexOf(");")` walker; refactored to parenthesis-balance walking + added positive `.DisableRateLimiting()` anti-regression assertion.
- **A1** AC-1 Quality-Gates row credited `_AC1` (HttpContext.Items proxy only) for "LogContext push verified". Updated to cite `_AC1 + _AC1b` together.
- **A6 + A7** AC-9 + AC-6 sub-items were aggregate-claimed in Quality-Gates (4 + 5 sub-items collapsed to single rows). Expanded per A29 to per-sub-item enumeration.

## What went well (Continue)

1. **Bulk create-story → bulk dev-story → boundary review → retro single-session flow** per Harry's autonomous-mode directive worked cleanly. ~1380 LOC + 25 new tests + 14 patches all converged in one continuous run. Compare to E14 (5 stories, also single session): pattern is now well-established.

2. **A51 direct-artifact-read pattern continues to deliver A49-immune tests.** Every E17 test (25 of 25) is code-audit-only via `File.ReadAllText` + regex/structural extraction; zero runtime tests blocked. Same pattern E14 used; same pattern future stories will use until A49's Serilog re-entrancy refactor lands.

3. **A38 doc-bundle pattern shipped 3 sections (~540 lines) cleanly.** Sections 25 + 26 + 27 inserted between Section 24 (E14-S5) and the Appendix in canonical sequence. Cross-section cross-references audited by Blind Hunter; 2 anchor issues found + patched.

4. **A47 uniform escape application across 3 stories surfaced 14 `[!]` items as one walkthrough queue** instead of scattered "needs Beta deploy" mid-implementation pauses. Q-list per story + operator-readable in Completion Notes.

5. **A41 autonomous-mode escape was clean for 7 DEC-Needed surfaces** with (a)/(b)/(c) Debug Log per A43. Acceptance Auditor confirmed all 7 had: option chosen + story-recommendation + user-directive verbatim quote + architectural justification + consequence chain.

6. **Decision to use TestCorrelator (already added in E14-S5) instead of introducing Serilog.Sinks.InMemory paid off.** Zero new dependencies; pattern parity with `SensitiveDataDestructuringPolicyTests`; sidesteps A49 cleanly.

## What hurt (Stop)

1. **AC-5 InMemoryCollection guard shipped as theatre.** The empty `Dictionary<string,string?>` did nothing; the XML doc claimed A36 protection that wasn't actually engaged. P5/E2 boundary-review patches fixed it. **Process gap:** the A36 pattern should be one canonical helper (or example) referenced by every test reading env-var-mapped IConfiguration. **Promoting to A53** (see action items).

2. **AC-9 + AC-6 sub-items aggregate-claimed in Quality-Gates table.** A29 specifies per-sub-item enumeration. Dev-agent collapsed multi-item ACs to single deferred rows. Boundary-review A6 + A7 patches expanded them. **Process gap:** A29 should explicitly cover `deferred-pending-beta-green` rows, not just `covered`. **Promoting to A54.**

3. **Test path-resolution uses brittle 5/6-level `..` traversal.** Same precedent as E14-S2 SecurityHeadersTests — works for the standard `dotnet test` from `backend/` but breaks under `--output ./custom-output` and CI containerization. **Deferred as E17-FT-2** with ancestor-walking helper proposed. Will revisit before CI changes to containerized test runs.

4. **A52 endpoint-pattern verification missed in E17-S2 refresh notes.** Notes claimed "A52 N/A" but AC-12 walkthrough Q-items explicitly name `/health/ready`. Acceptance Auditor caught + A9 patch acknowledged the cross-story verification (via E17-S4 Task 0). **Process gap:** A52 should be re-checked at the per-story Quality-Gates close, not just at refresh time.

## What's missing (Start)

1. **Promote canonical A36 test shape to project-context.md (A53 candidate).** A reusable "ConfigurationBuilder + AddEnvironmentVariables + AddInMemoryCollection-with-populated-bindings" pattern with explicit non-empty-dict + non-skipped-envvars guards. Surfaced by AI-1 in boundary review.

2. **Reinforce A29 enumeration discipline for deferred rows (A54 candidate).** A29 currently reads as "explicit per-sub-item evidence" — implicitly only for `covered` rows. Extend to "explicit per-sub-item evidence INCLUDING `deferred-pending-beta-green` rows."

3. **Test-infrastructure-hardening mini-epic (Process candidate).** Epic-17 alone added 14 deferred entries; several (E17-FT-1 anchor registry, E17-FT-2 ancestor-walking helper, E17-FT-4 row-level table parity) are reusable test-infrastructure. Consider scheduling a one-off test-infra commit before E18, OR a "Test Infra Hardening" mini-epic post-Beta-green.

4. **A52 reinforcement: re-check at Quality-Gates close, not just refresh.** A52 was authored to catch SCP-vs-code endpoint divergence at refresh time. E17-S2's "A52 N/A" claim was correct at refresh time (SCP AC text didn't name endpoints) but wrong by close (AC-12 walkthrough refined to name `/health/ready`). Surface as A55 candidate.

## Action items to promote to project-context.md

| ID | Description | Source |
|---|---|---|
| **A53** | Canonical A36 test shape: `ConfigurationBuilder + AddJsonFile + AddEnvironmentVariables + AddInMemoryCollection-with-POPULATED-bindings`. Empty-dict InMemoryCollection without AddEnvironmentVariables is theatre — A36 protection requires both. | E17 boundary review P5/E2 |
| **A54** | A29 AC-Subitem Completion Check extends to `deferred-pending-beta-green` rows. Live-walkthrough ACs enumerating N sub-items must produce N Quality-Gates rows, not one aggregate. | E17 boundary review A6 + A7 |
| **A55** | A52 endpoint-pattern verification re-runs at Quality-Gates close, not just refresh. ACs that get refined to name endpoints during dev-story (AC text grows beyond the SCP's literal text) need to be re-audited against the codebase before status flips. | E17 boundary review A9 |

(A28-A52 carry forward from Epic-11/12/13/14/15/16/20 retros.)

## Status transitions executed

- e17-s1-restrict-serilog-to-console-in-containers: `review` → `done`
- e17-s2-validate-structured-logs-with-correlation-id: `review` → `done`
- e17-s4-add-external-uptime-monitoring: `review` → `done`
- epic-17: `in-progress` → `done`
- epic-17-retrospective: `optional` → `done`

## Sprint state after E17 close

- **E1-E3 + E9-E17 + E20** all done (12 epics closed)
- **E18 + E19** backlog (Wave-9/10 — Beta operations + Production prep)
- **E4-E8** still deferred backlog (pre-Beta-pivot epics — resume after Beta validation)

**Next per workflow.on_complete:** next in-progress epic with ready-for-dev stories = NONE. Per SCP-2026-05-15 §6 wave order, **Wave 9 = E18 Beta Test Preparation and Operations Documentation** (4 stories — RUNBOOK-beta + tester onboarding guide + BETA banner in UI + feedback channel).

**Recommendation per session-pacing-dev-cycles + A34:** commit this Epic-17 close (5 new test files + 3 docs sections + boundary review + retro + project-context.md A53-A55 update + deferred-work.md +14) and start `bmad-create-story` for entire E18 in a fresh session.

Per project memory feedback_session_pacing_dev_cycles + A34, recommend separate sessions per dev-story for E18 (4 stories).
