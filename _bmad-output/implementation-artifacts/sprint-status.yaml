# generated: 2026-05-12
# last_updated: 2026-06-01 (bmad-create-story BULK REFRESH for entire Epic-15 (Database, Persistence,
#   and Migrations): COMPLETE. All 4 stories (e15-s1..s4) authored or refreshed in one session per A34
#   (bulk spec-refresh at epic start when N≥3 stories in one session). **Refresh deltas:** (1) e15-s1
#   (verify two-Postgres separation) — LIGHT refresh: 2026-05-15 draft was 284 lines + comprehensive;
#   added a Refresh Notes block confirming all docs/14_beta_railway_setup.md section anchors (3.2, 3.4,
#   5.1, 8.2, 8.3) stable post-E13-close at fc085e2; clarified Section 14 inserts BETWEEN Section 13 +
#   Appendix (not literally "after Section 13" which was ambiguous); added Q5 about Sections 14+15+16
#   forming a coherent "Beta validation & operations" cluster. Status unchanged: ready-for-dev. (2)
#   e15-s2 (Database__AutoMigrate toggle) — FULL AUTHOR from 19-line stub: 8 ACs + 9 Tasks. Critical
#   delta: Database__AutoMigrate=true ALREADY in backend/.env.example:31 (from E11-S1); the story is
#   the IMPLEMENTATION (wire Program.cs:51-90 to read the toggle), NOT a template-refresh. Two
#   MigrateAsync call-sites at Program.cs:56 (Development) + Program.cs:88 (Production); Testing
#   branch at line 46-50 uses EnsureCreatedAsync and is excluded. Helper extraction matches the
#   RegisterRetentionEnforcementJob precedent (DependencyInjection.cs:379-401). Status: backlog →
#   ready-for-dev. (3) e15-s3 (daily Postgres backup to RustFS) — HEAVY refresh: 2026-05-15 draft was
#   270 lines but contained two material drift-from-code issues. (a) ScheduledBackupJob class ALREADY
#   EXISTS at backend/src/IabConnect.Infrastructure/Backup/ScheduledBackupJob.cs (33 lines) — the
#   prior draft assumed a new RegisterBackupJobs.cs would be created. Correction: reuse existing job
#   class; only Hangfire registration (currently absent — confirmed by grep of DependencyInjection.cs:
#   298-334 showing 4 jobs registered, ScheduledBackupJob missing) gets added via a new
#   RegisterDailyBackupJob static helper mirroring RegisterRetentionEnforcementJob. (b) IBackupService
#   consumer surface is broader than prior draft acknowledged — GetBackupFileAsync, RestoreBackupAsync,
#   DeleteBackupAsync, UploadBackupAsync all assume local-filesystem semantics; the refactor must
#   preserve OR redirect them. Story added DEC-1 (A32 Decision-Needed) block surfacing 3 options for
#   local-vs-RustFS storage semantics; Option A (Hybrid local + RustFS) RECOMMENDED. Also confirmed
#   exactly 6 docker-call sites at lines 66/90/116/212/233/260 of PostgresBackupService.cs. Story
#   expanded from 270 → ~480 lines. Status unchanged: ready-for-dev. (4) e15-s4 (Beta seeding
#   strategy) — FULL AUTHOR from 19-line stub: 6 ACs + 6 Tasks. Key DEC-1 (A32 Decision-Needed)
#   block: doc-target = docs/14_beta_railway_setup.md Section 16 (Option A, A38 doc-bundle,
#   RECOMMENDED) vs. new docs/RUNBOOK-beta.md (Option B, matches SCP source text verbatim) vs.
#   defer-to-E18-S1 (Option C, blocks Wave-7 closer). Confirmed 7 realm-roles in
#   infra/keycloak/realms-beta/iabconnect-realm.json:163-198 (mfa-required, admin, vorstand, member,
#   kassier, auditor, event-manager). Confirmed DevelopmentDataSeeder is static class with NO internal
#   gate (gate at Program.cs:51 wrapping line 63 call). Source REQ is REQ-088 AC-10 (was incorrectly
#   AC-3 in stub). Status: backlog → ready-for-dev. **Cross-cutting findings:** (a) docs/14_beta_railway_setup.md
#   gains 3 new sections during E15: Section 14 (E15-S1) + Section 15 (E15-S3) + Section 16 (E15-S4,
#   IFF DEC-1=A) — all inserted BETWEEN Section 13 + existing Appendix. (b) Two distinct DEC-1
#   surfaces (one in e15-s3, one in e15-s4) — both scoped within their own story file; dev-agent
#   surfaces via AskUserQuestion per project memory feedback_decisions_via_ask_tool; A41
#   autonomous-mode escape applies if user pre-declares. (c) ScheduledBackupJob exists-but-unregistered
#   is a latent gap likely created during pre-Beta-pivot REQ-053 work — E15-S3 finally closes the
#   wiring under REQ-088 AC-6. (d) `Database__AutoMigrate=true` ALREADY in env.example:31 (E11-S1)
#   means E15-S2 is purely implementation; doc-only updates exist (Section 5.1 row). (e) The 7-role
#   verification in E15-S4 AC-2 is `[!] needs-human-verify` per A30 — dev-agent cannot reach a
#   live Beta Keycloak Admin Console. **Status transitions:** epic-15 in-progress (unchanged);
#   e15-s1 ready-for-dev (unchanged, light refresh); e15-s2 backlog → ready-for-dev;
#   e15-s3 ready-for-dev (unchanged, heavy refresh); e15-s4 backlog → ready-for-dev.
#   Sprint state: E1-E3 + E9-E12 + E13 + E20 all done + E15 in-progress (4 ready-for-dev).
#   NEXT: per workflow.on_complete + project memory feedback_session_pacing_dev_cycles + A34,
#   recommend committing this 4-story bulk-context refresh + sprint-status update and starting
#   bmad-dev-story for e15-s1 (Wave-7 opener) in a fresh session. IMPORTANT: e15-s1 Task 0.2
#   prerequisite `[!]` is "Beta deploy GREEN" — Harry must confirm the ~60 [!] items from
#   docs/14_beta_railway_setup.md are executed + browser smoke green BEFORE e15-s1 dev-story
#   begins. Without this, Tasks 1-12 cannot run. Once e15-s1 done, the natural sequence is s2
#   (independent code change; can run in parallel with s1), then s3 (depends on s1), then s4
#   (depends on s1+s3 + DEC-1 resolution). All 4 dev-story runs deliberately recommended as
#   separate sessions per feedback_session_pacing_dev_cycles.) ## Previously: 2026-06-01 (bmad-create-story BULK REFRESH for entire Epic-13 (Railway Beta
#   Deployment): COMPLETE. All 4 stub files (e13-s1..s4, previously 19-line generate-beta-stubs.sh
#   output) replaced with ~400-500 line dev-ready stories per A34 (bulk spec-refresh at epic
#   start when N≥3 stories in one session). **Refresh deltas vs. 2026-05-15 stubs:** (1)
#   e13-s1 grew to 11 ACs + 10 Tasks; concretized the six-service topology (3 public + 3
#   private — note: 6 services, NOT the 5 the epic-source AC-2 text says, because the epic
#   text bundles the two Postgres instances as "plus two managed Postgres" which is
#   functionally 6 services on Railway); enumerated the 4 BLOCKING [!] prerequisites (DCO
#   branch protection, 12 GHA repo variables, first GHCR publish trigger, 3 GHCR packages
#   → Public visibility); resolved the chicken-and-egg for `_BETA` GHA repo variables
#   (Railway-assigned hostnames are the output of E13-S1 Task 2; Harry updates the GHA
#   variables BETWEEN E13-S1 Task 2 and the next push); seeded the Keycloak JDBC env block
#   in E13-S1 (NOT E13-S2) because Keycloak crash-loops without it and an empty env surface
#   is hard to diagnose. (2) e13-s2 grew to 11 ACs + 8 Tasks; full env-var enumeration per
#   service (api ~30 vars, web ~5 runtime, keycloak ~10 KC_* + 3 realm-secret placeholders);
#   classified build-time-baked (NEXT_PUBLIC_*, BUILD_SHA/DATE) vs runtime (everything else)
#   so the dev-agent doesn't try to set NEXT_PUBLIC_API_URL on Railway expecting it to take
#   effect; documented the KEYCLOAK_ISSUER ≡ NEXT_PUBLIC_KEYCLOAK_ISSUER ≡ Keycloak__Authority
#   3-anchor parity invariant that breaks login silently when drifted; documented the
#   three-way client-secret sharing pattern across realm-import placeholder ↔ Keycloak DB ↔
#   consumer env var. (3) e13-s3 grew to 10 ACs + 9 Tasks; verifies the 3-public-3-private
#   topology with explicit curl + psql tests from outside Railway; confirms Beta runs the
#   production-hardening branch (HSTS, HTTPS-redirect, strict CORS) per ADR-015; documented
#   the CORS hostile-origin preflight test (`https://evil.example.com` Origin → no ACAO
#   echo). (4) e13-s4 grew to 9 ACs + 8 Tasks **AND surfaces DEC-1 (Decision-Needed at
#   dev-story start)**: the `web` service AC-2 healthcheckPath is `/api/health`, but the
#   producer endpoint does NOT exist in frontend/ (verified by `git ls-files
#   frontend/src/app/api/health` = 0 files). The producer story E17-S3 is Wave 8 → 2 waves
#   AFTER E13-S4 (Wave 6) — wave-order inconsistency. 3 options surfaced in-story (A: in-line
#   the 5-line route here, RECOMMENDED; B: pull E17-S3 forward; C: use `/` as healthcheck
#   instead). Decision is per-A32 (`Decision-Resolution with Manual-Verify Hand-off`) —
#   dev-agent MUST surface via AskUserQuestion at Task 0.2 before further work; resolution
#   recorded in Dev Agent Record. **Cross-cutting findings during refresh:** (a) ADR-012 graphic
#   says "Five application services" but enumerates 6 (web, api, keycloak, rustfs, postgres-app,
#   postgres-kc) — the "five" comes from {web, api, keycloak, rustfs, postgres-{app+kc}-as-one};
#   stories use 6 throughout for clarity and surface the language alignment as a Q. (b) Keycloak
#   26.5.2 + `--optimized` + KC_DB=postgres baked at image-build (E12-S3) means runtime needs
#   KC_DB_URL/USERNAME/PASSWORD + KC_HOSTNAME + KC_PROXY=edge + KC_HTTP_ENABLED=true; ADR-016
#   and the Wave-3 dockerfile do NOT enumerate these — e13-s2 AC-4 makes them explicit. (c)
#   Beta runs HSTS + HTTPS-redirect + strict CORS per ADR-015 (`IsDevelopment()` only is
#   Dev-only) — e13-s3 Tasks 5 and 6 turn this into live verification on first deploy. (d) The
#   project's E20-derived A34 (bulk spec-refresh at epic start) is the EXACT pattern this session
#   applied, validating the action item one epic later. **Status transitions:** epic-13 backlog →
#   in-progress; e13-s1 backlog → ready-for-dev; e13-s2 backlog → ready-for-dev; e13-s3 backlog →
#   ready-for-dev; e13-s4 backlog → ready-for-dev. Sprint state: E1-E3 + E9-E12 + E20 all done +
#   E13 in-progress (4 ready-for-dev). NEXT: per workflow.on_complete + project memory
#   `feedback_session_pacing_dev_cycles`, recommend committing this 4-story bulk-context
#   refresh + sprint-status update and starting bmad-dev-story for e13-s1 in a fresh session.
#   IMPORTANT before dev-story starts: Harry must complete the 4 BLOCKING [!] preparation
#   tasks first per e13-s1 prerequisite block — (1) DCO branch protection on main+beta;
#   (2) 12 GHA repo variables populated (some values are chicken-and-egg with E13-S1 Task 2,
#   see e13-s1 Task 0.2); (3) first GHCR publish workflow run successful on `beta`; (4) flip
#   3 GHCR packages to Public visibility via GitHub UI. These are Harry-only and the
#   dev-agent CANNOT execute them.) ## Previously: 2026-06-01 (Epic-20 CLOSED — Open Source Foundation. Single continuous
#   session delivered all 5 stories + epic-boundary code-review + retrospective. **Epic
#   summary:** project becomes public-OSS-ready at merge. AGPL-3.0-or-later LICENSE +
#   COPYRIGHT + NOTICE.md (with 32 frontend + 27 backend direct deps inventoried) +
#   CONTRIBUTING.md (DCO sign-off + SPDX policy) at repo root; `.github/workflows/dco.yml`
#   (inline bash `git rev-list --no-merges` over `Signed-off-by:` regex, ZERO third-party
#   action dependency for DCO since dcoapp/action 404'd at impl time; only `actions/checkout`
#   pinned by SHA); `.github/workflows/build-images.yml` (matrix workflow publishing 3 images
#   `ghcr.io/htos/iabc-{api,web,keycloak}` with 7 OCI provenance labels including
#   `org.opencontainers.image.licenses=AGPL-3.0-or-later`; 5 actions pinned by 40-char SHA;
#   concurrency-grouped per ref to prevent `:beta` tag race); backend `/about` endpoint
#   (REQ-089 AC-5 / ADR-021 — unauthenticated, 6 camelCase fields, BUILD_SHA/BUILD_DATE
#   from Dockerfile build-args, sourceUrl from Branding:SourceUrl options); frontend
#   universal `<LicenseFooter />` mounted at root layout + `/public/license` static page
#   with embedded LICENSE text + gnu.org fallback. **Quality gates:** dotnet build 0
#   warnings 0 errors; backend test suite 1963 → **1976** green (+13 new for e20-s3);
#   frontend test suite 127 → **135** green (+8 new for e20-s4); npm typecheck 0 errors;
#   npm lint 2 baseline errors unchanged (members/segments/page.tsx pre-existing); npm
#   build green with `/public/license` route registered. Live smoke curl GET /about HTTP
#   200 application/json with all 6 camelCase fields. **Epic-boundary code review** (3
#   adversarial layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor) over the
#   ~1207-line diff produced 19 unique findings → **4 patches APPLIED** in this session:
#   P1 NOTICE.md QuestPDF license accuracy (was mislabeled MIT; real license is QuestPDF
#   Community License revenue-gated under USD 1M); P2 DCO workflow added `--no-merges`
#   to git rev-list so contributors syncing main into long-lived PR branches don't trip
#   the check on the merge commit; P3 build-images.yml `concurrency: { group: ...,
#   cancel-in-progress: false }` to prevent `:beta` tag race on rapid pushes; P4
#   TestWebApplicationFactory ConfigureAppConfiguration now binds `BUILD_SHA=""` and
#   `BUILD_DATE=""` in InMemoryCollection so the About_UnknownWhenEnvVarsMissing
#   integration test stays env-independent when CI runner has BUILD_SHA exported. All
#   13 About tests re-ran green after patches; full backend suite re-ran 1976/1976
#   green. 11 defers logged in deferred-work.md under "code review of Epic-20 boundary
#   (2026-06-01)" — partitioned per source story (e20-s1: 4 D1-D4 DCO + checkout-sha;
#   e20-s4: 5 D1-D5 LicensePage fs/per-request + self-link + comment-key leak + version
#   disclosure + real-build verify; e20-s5: 3 D1-D3 BUILD_DATE fallback + per-matrix
#   build-args isolation + revision/created label verify). 4 dismisses (next-env.d.ts
#   transient regen, NEXT_PUBLIC_SOURCE_URL intentional hardcode, AboutEndpoints.cs
#   InternalsVisibleTo Blind-Hunter false-positive, Quellcode-vs-Source intentional
#   translation). **Epic-20 retrospective:** doc saved at
#   _bmad-output/implementation-artifacts/epic-20-retro-2026-06-01.md. A28-A33 carried
#   forward: A28 spike-first strongly applied (e20-s3 Task 0 confirmed 8 anchors before
#   coding); A29 AC-subitem completion check strongly applied (e20-s3 Quality-Gates
#   table with 21 explicit row statuses); A30 [x]/[!]/[ ] convention strongly applied
#   (8 [!] items now in epic-close human-verify queue); A31 cross-story orthogonal-AC
#   inventory strongly applied (Acceptance Auditor confirmed 14-anchor sourceUrl
#   convergence + 8-anchor license-string parity zero drift); A32 N/A (no Decision-Needed
#   at boundary); A33 N/A (no new Dockerfile work). **4 new action items A34-A37
#   promoted to project-context.md** under Story Authoring & Dev-Story Execution Rules:
#   A34 Bulk spec-refresh at epic start for N>=3 stories in one session (prevents
#   late-discovered dcoapp/action 404 + README license-contradiction surprises);
#   A35 Frontend Vitest + Testing Library requires explicit afterEach(cleanup)
#   (surfaced by LicenseFooter.test.tsx initial 4-failure cycle); A36 ASP.NET Core
#   integration tests reading env-var-mapped IConfiguration must override in
#   TestWebApplicationFactory via AddInMemoryCollection empty bindings (P4 patch
#   rationale); A37 license-related changes must grep README for contradictory
#   prose (E20-S1 shipped LICENSE while README still said "proprietary software ...
#   All Rights Reserved"; E20-S2 fixed as scope addition). **Beta-deploy readiness
#   gate:** E13 prerequisites code-side ALL GREEN. 8 [!] human-verify items block E13;
#   4 of those BLOCKING: (1) Harry configures GitHub branch protection requiring `DCO`
#   check on main+beta; (2) Harry configures 12 GHA repo variables NEXT_PUBLIC_*_BETA
#   + _MAIN per build-images.yml header; (3) Harry merges PR + triggers first GHCR
#   publish; (4) Harry flips 3 GHCR packages to Public visibility via GitHub UI. **No
#   significant discoveries** require E13 epic-plan update — SCP-2026-05-15 Beta-on-
#   Railway pivot remains aligned. Status transitions: e20-s1/s2/s3/s4/s5 review -> done;
#   epic-20 in-progress -> done; epic-20-retrospective optional -> done. Sprint state:
#   E1-E3 + E9-E12 + E20 all done. NEXT per workflow.on_complete: next in-progress epic
#   with ready-for-dev stories = NONE (all remaining epics backlog); per SCP-2026-05-15
#   wave order, Wave 6 starts with E13 (Railway Beta Deployment), first story
#   e13-s1-create-railway-project-and-services. RECOMMEND: (a) Harry completes the 4
#   BLOCKING [!] preparation tasks first (DCO branch protection + GHA vars + first
#   publish + public package visibility); (b) bmad-create-story for e13-s1 to refresh
#   its context against post-E20 reality. Per project memory feedback_session_pacing_dev_cycles
#   and A34, the next session should run bulk create-story for all 4 E13 stories upfront
#   before any dev-story.) ## Previously: 2026-06-01 (bmad-dev-story for e20-s3-add-backend-about-endpoint: COMPLETE
#   — Wave-4 first story (REQ-089 AC-5 / ADR-021 AGPL §13 source-disclosure). 5 new files:
#   `backend/src/IabConnect.Infrastructure/Common/BrandingOptions.cs` (17 lines, new
#   `Common/` folder + new `IabConnect.Infrastructure.Common` namespace),
#   `backend/src/IabConnect.Api/Endpoints/AboutEndpoints.cs` (74 lines, with nested
#   `AboutResponse` record matching `SettingsEndpoints.cs:397-437` precedent — story
#   spec said 5 new .cs files but `AboutResponse.cs` was collapsed into the endpoint
#   file per Task 0.2 spike decision → reality is 4 new .cs files),
#   `backend/tests/IabConnect.Api.Tests/Endpoints/AboutEndpointTests.cs` (7 [Fact]
#   integration tests via shared TestWebApplicationFactory + [Collection("Api")]),
#   `backend/tests/IabConnect.Api.Tests/Endpoints/AboutResponseBuilderTests.cs` (2
#   [Fact] + 1 [Theory] with 4 inline-data rows = 6 pure unit tests over the
#   internal BuildResponse projection helper, reaches the `internal` member via
#   the existing `[assembly: InternalsVisibleTo("IabConnect.Api.Tests")]` at
#   DependencyInjection.cs:16 — no `.csproj` change needed). 3 edited files:
#   `appsettings.json` +3 lines (new "Branding": { "SourceUrl": ... } block between
#   Frontend and DocumentStorage), `DependencyInjection.cs` +6 lines (using directive +
#   services.Configure<BrandingOptions> registration with REQ/E20-S3/ADR comment;
#   placed adjacent to ConfigureHttpJsonOptions in the configuration-binding region of
#   AddApiServices), `EndpointMapper.cs` +1 line (app.MapAboutEndpoints() insertion
#   at line 17 in the Public Endpoints block, on app NOT api — root-path registration
#   per AC-1). **Quality gates:** dotnet build 0 warnings 0 errors; dotnet test full
#   suite 1442 + 145 + 389 = 1976/1976 green (baseline 1963 + 13 new = 1976, zero
#   regressions). Local Kestrel smoke at http://localhost:5000/about returned HTTP
#   200 Content-Type application/json; charset=utf-8 with all 6 camelCase keys
#   present (`{"name":"IAB Connect","license":"AGPL-3.0-or-later","version":"1.0.0.0",
#   "commitSha":"unknown","buildDate":"unknown","sourceUrl":"https://github.com/htos/iab-connect"}`),
#   plus the same body when curl-ed with `Authorization: Bearer not-a-real-token`
#   (HTTP 200 NOT 401 — proves .AllowAnonymous() neutralizes bad tokens, AC-3 satisfied).
#   **Cross-story orthogonal-AC parity (A31) verified:** `AGPL-3.0-or-later` byte-identical
#   across backend/Dockerfile:46+48 (OCI labels) + frontend/Dockerfile:102+104 +
#   infra/keycloak/Dockerfile:23+25 + new AboutEndpoints.cs:1+62. `https://github.com/htos/iab-connect`
#   converges across 12 anchors: 3 Dockerfile OCI source labels + backend/.env.example:100
#   + frontend/Dockerfile:56 (ARG default) + frontend/.env.example:64 +
#   BrandingOptions.cs:16 (new default) + appsettings.json:38 (new section) +
#   BetaBanner.tsx:74 (frontend fallback) + BetaBanner.test.tsx:80 (existing test) +
#   2 new test assertions. SPDX header on line 1 of all 4 new .cs files. JSON casing
#   camelCase confirmed in curl body. **Two [!] markers (project-context A30) queued for
#   human/post-Beta verify:** Task 9.5 (incognito-browser confirmation of CORS/no-redirect)
#   + Task 9.6 (Docker container build with concrete --build-arg BUILD_SHA + BUILD_DATE
#   to confirm the OS-level ENV→process bridge). Both are documented in the Quality-Gates
#   table with evidence anchors; the spec contract (build-arg names + IConfiguration
#   projection) is covered by Task 8 unit test + Task 0.6 spike. **One forward-looking
#   parity row [!]-deferred:** LICENSE/COPYRIGHT/CONTRIBUTING.md files do NOT yet
#   exist at repo root because E20-S1 + E20-S2 are still `ready-for-dev` — parity will
#   be enforced byte-comparing my AboutEndpoints.cs:62 license literal against
#   E20-S1's LICENSE when that story lands. **Story-vs-reality deviation reviewer should
#   see:** test count for Task 8 grew from 2 → 6 (added a [Theory] with null/empty/
#   whitespace inline-data rows for the ReadOrUnknown helper contract). Bonus coverage,
#   not spec drift — the original 2 named scenarios (override + default) both ship.
#   **Test-runtime constraint encountered + resolved:** initial dotnet test run failed
#   7 API tests because Docker Postgres (port 5433) wasn't up; HealthEndpointTests
#   (existing) failed identically — confirmed NOT a regression from this story.
#   Brought up Postgres via `docker compose up -d postgres` in `infra/`; all 13 new
#   tests then passed. Status transitions: e20-s3 ready-for-dev → in-progress → review.
#   Sprint state: E20 in-progress (4 ready-for-dev + 1 review — e20-s3); E11/E12 done.
#   NEXT per workflow.on_complete hybrid policy: NOT all E20 stories in review (4
#   still ready-for-dev: e20-s1, e20-s2, e20-s4, e20-s5), so recommend bmad-create-story
#   for the next ready-for-dev story in sprint order. Per Wave-4 dependency: e20-s4
#   (frontend license footer) is the natural next pick — it consumes /about and the
#   NEXT_PUBLIC_SOURCE_URL build-arg pattern this story just baked into appsettings,
#   AND it closes Wave 4. After e20-s4, Wave 1 cleanup (e20-s1 LICENSE/DCO + e20-s2
#   SPDX policy) closes the E20 documentation tail, then Wave 5 (e20-s5 GHCR pipeline)
#   finishes the epic. Per project memory feedback_session_pacing_dev_cycles, recommend
#   committing this dev-story state and starting the next dev-story in a fresh session.)
#   ## Previously: 2026-06-01 (bmad-create-story REFRESH for e20-s3-add-backend-about-endpoint:
#   COMPLETE. Story file at _bmad-output/implementation-artifacts/e20-s3-add-backend-about-endpoint.md
#   re-authored against post-Epic-11/Epic-12 reality. Status stays ready-for-dev (was
#   already ready-for-dev from the 2026-05-15 bulk create-story pass). Key refresh
#   deltas vs. 2026-05-15 draft: (1) line anchors re-aligned to current code —
#   EndpointMapper.cs insertion at line 17 (was vague), appsettings.json Frontend
#   block at 34-36 / DocumentStorage 37-43 (was off by ~10 lines), HealthEndpointTests
#   body-read at line 39 (was line 36); (2) DTO collapsed INTO AboutEndpoints.cs as
#   nested record matching SettingsEndpoints.cs:397-437 precedent (drops the
#   AboutResponse.cs separate-file ambiguity); (3) BrandingOptions namespace moved
#   from IabConnect.Infrastructure.Options to IabConnect.Infrastructure.Common
#   (Common/ folder new; existing precedent is per-feature like
#   Infrastructure.Events.CalendarTokenOptions, so Common/ is the cross-cutting
#   bucket); (4) configuration-binding test MOVED from Infrastructure.Tests to
#   Api.Tests because InternalsVisibleTo at DependencyInjection.cs:16 is granted
#   ONLY to IabConnect.Api.Tests (NOT Infrastructure.Tests) — earlier draft was wrong;
#   (5) Task 0 SPIKE added per A28 with 8 sub-confirmations (EndpointMapper line,
#   DTO convention, InternalsVisibleTo scope, appsettings line range, Branding:
#   lookup state, Dockerfile build-args + OCI label parity, Kestrel local port,
#   spike output); (6) Task 9 manual-validation re-formatted with [x]/[!] markers
#   per A30 (4 dev-agent-automatable curl/dotnet steps as [x] + 2 human-verify
#   browser/docker-container steps as [!]); (7) Task 10 added — cross-story
#   orthogonal-AC verification per A31 (license-string grep parity across LICENSE/
#   COPYRIGHT/Dockerfile-label/endpoint-literal; sourceUrl-default grep parity
#   across backend BrandingOptions/appsettings/.env.example/frontend Dockerfile/
#   BetaBanner.tsx; SPDX-header presence on all 5 new .cs files; JSON casing
#   camelCase); (8) Task 11 added — Quality-Gates Closing Check table per A29
#   with 21 explicit AC-sub-item rows (no aggregate "all 7 verified" claims).
#   Build-arg contract framing inverted: 2026-05-15 draft framed BUILD_SHA/BUILD_DATE
#   as "downstream / not yet shipped" — now framed as "upstream shipped, this
#   story consumes via IConfiguration env-var binding which ASP.NET Core resolves
#   for free, no AddEnvironmentVariables() needed". Sprint state: E20 in-progress
#   (5 ready-for-dev including this refreshed e20-s3) + E12 done + E11 done.
#   NEXT: bmad-dev-story for e20-s3 to start Wave-4 implementation. The story
#   file IS the dev-agent's master guide — Task 0 spike output (one line) is the
#   first dev-agent emission. Per project memory feedback_session_pacing_dev_cycles,
#   recommend committing this story refresh + sprint-status update and starting
#   the dev-story in a fresh session.) ## Previously: 2026-05-16 (bmad-retrospective for Epic-12: COMPLETE. Retro doc saved
#   at epic-12-retro-2026-05-16.md. 3 new action items A31 (Cross-Story Orthogonal-AC
#   Inventory) / A32 (Decision-Resolution with Manual-Verify Hand-off) / A33
#   (Base-Image User Verification in Dockerfile Spikes) — all promoted to
#   project-context.md as Rules under "Story Authoring & Dev-Story Execution Rules
#   (from Epic-11 retro A28-A30, 2026-05-16; Epic-12 retro A31-A33, 2026-05-16)".
#   A28/A29/A30 follow-through evaluated: A28 strongly applied (all 4 stories had
#   Task 0 spikes catching non-obvious constraints); A29 partially applied (worked
#   for explicit-numbered ACs, missed cross-story orthogonal dimensions like
#   "no dev secrets in image" — drives A31); A30 strongly applied (4 [!] markers
#   correctly queued, but D3 showed that [!]-deferred verification can also block
#   Decision resolution — drives A32). A27 from E10 carries forward (still N/A in
#   E12; will trigger when E13/E14 require a long-running stack mid-review). No
#   Significant Discoveries invalidate Wave-4 plans. epic-12-retrospective status
#   optional -> done. Sprint state: epic-11 done + epic-12 done + epic-20
#   in-progress (5 ready-for-dev). Per workflow.on_complete: NEXT is
#   bmad-create-story for the first Wave-4 story = e20-s3-add-backend-about-endpoint
#   (already ready-for-dev, story file exists from the bmad-create-story pass on
#   2026-05-15). Since the story file exists with full context, NEXT can also be
#   bmad-dev-story for e20-s3 directly — choice surfaced to user. Per project
#   memory feedback_session_pacing_dev_cycles, recommend committing this retro
#   close + A31-A33 project-context update and starting the dev-story in a fresh
#   session.) ## Previously: 2026-05-16 (bmad-code-review for Epic-12 boundary: COMPLETE. Adversarial
#   review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) over the full
#   E12 diff (~924 lines code/config across 13 source files). Triage produced
#   3 decision-needed, 7 patches, 28 defers, ~17 dismisses. All 3 decisions
#   resolved: D1 → (A) accept ASPNETCORE_ENVIRONMENT=Development + README disclaimer
#   expansion (P6); D2 → (B) replace `*.up.railway.app/*` wildcard with
#   ${IABCONNECT_BETA_HOST}/* placeholder (new P8); D3 → (A) verify NEXTAUTH
#   issuer divergence via browser smoke first (queued behind Task 8 [!]). All 8
#   patches applied in this session: P1 backend USER 1000→$APP_UID + chown app:app;
#   P2 **/appsettings.Development.json added to backend/.dockerignore (literals no
#   longer ship in published OCI image); P3 NEXT_PUBLIC_* fail-fast guard before
#   `npm run build`; P4 `npm install -g npm@11.6.0` patch pin; P5 HEALTHCHECK on
#   both Dockerfiles + overlay depends_on flipped to service_healthy + api gated on
#   keycloak-full-realm-check.service_completed_successfully; P6 README Option 4
#   "What this overlay does NOT simulate" disclaimer; P7 README Option 3 ellipsis
#   replaced with concrete example values for all 5 required NEXT_PUBLIC_* args;
#   P8 sanitized realm `iabconnect-frontend` redirectUris + webOrigins use
#   ${IABCONNECT_BETA_HOST}/* placeholder (overlay supplies BETA_HOST=localhost:3000
#   for local smoke; E13-S2 sets it to the actual Railway frontend URL). 28 defers
#   logged in deferred-work.md under "Deferred from: code review of Epic-12
#   boundary (2026-05-16)" — partitioned per source story (e12-s1: 8 entries D1'-D8',
#   e12-s2: 3 entries D9'-D11', e12-s3: 7 entries D12'-D18', e12-s4: 12 entries
#   D19'-D30'). Quality gates: dotnet build 0 warnings/0 errors; docker compose
#   config -f base -f full green; JSON+YAML syntax valid post-patches. Status
#   transitions: e12-s1/s2/s3/s4 review → done; epic-12 in-progress → done. NEXT:
#   per workflow.on_complete + project memory `feedback_bmad_workflow` (hybrid:
#   CR+ER at epic boundary), bmad-retrospective for Epic-12 close. Then Wave-4
#   per SCP-2026-05-15 §6 starts: E20-S3 backend /about endpoint + E20-S4 frontend
#   license footer (both consume the BUILD_SHA/BUILD_DATE + NEXT_PUBLIC_SOURCE_URL
#   build-args that Wave-3 just baked into the images). Per project memory
#   `feedback_session_pacing_dev_cycles`, recommend committing this review state
#   and starting bmad-retrospective in a fresh session.) ## Previously: 2026-05-15 (Sprint re-planning post Beta-on-Railway and Open Source
#   Foundation pivot: bmad-sprint-planning re-run against the SCP-2026-05-15-aligned
#   epics-and-stories.md. 10 new epics (E11-E20) and 40 new stories added.
#   Status assignment uses the project's established wave-aware convention: file
#   presence does NOT auto-upgrade to ready-for-dev when the wave order parks a
#   story behind dependencies. Only Wave 1 stories (E20-S1, E20-S2 — both
#   dependency-free per SCP-2026-05-15 §6) are promoted to ready-for-dev. All
#   other 38 E11-E20 stubs stay backlog and will be promoted wave-by-wave during
#   sprint execution. Development-status block REORDERED to execution priority:
#   done epics (E1-E3, E9, E10) first, then the ACTIVE FOCUS (E20 with Wave 1
#   ready, then E11..E19 in approximate first-wave-appearance order), then the
#   DEFERRED backlog (E4-E8). Implementation readiness re-confirmed READY by
#   implementation-readiness-report-2026-05-15.md. NEXT: bmad-create-story for
#   e20-s1-add-license-dco-and-contributing — Wave 1 start.)
# project: iab-connect
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts
#
# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created via create-story
#   - in-progress -> done: Manually when all stories reach done
#
# Story Status:
#   - backlog: Story only exists in epic file (file may or may not be on disk)
#   - ready-for-dev: Story file created in implementation artifacts AND wave-order permits
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review
#   - done: Story completed
#   - deferred: Non-standard hold state — story consciously parked pending an external decision
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - This file tracks all open epics and stories from epics-and-stories.md.
# - Stories can be worked in parallel if team capacity allows.
# - Each story must go through bmad-create-story and validation before development.
# - The development_status block is ordered by EXECUTION PRIORITY, not epic number.
# - Wave-aware status: file presence does NOT auto-promote to ready-for-dev when
#   the wave order documented in epics-and-stories.md Release and Sprint Guidance
#   parks a story behind unmet dependencies.

generated: "2026-05-12"
last_updated: "2026-06-02 (Epic-15 CLOSED end-to-end in a single autonomous-mode session per user 'alle stories nacheinander ohne stop. erst ganz am schluss wird ein review und retro gemacht. wichtig es handelt sich nicht mehr um einen mvp' directive. **4 stories + epic-boundary code-review (3-agent adversarial: Blind Hunter + Edge Case Hunter + Acceptance Auditor) + epic-retrospective all delivered**. **Source artifacts**: E15-S1 doc Section 14 (~290 lines, 6 subsections, doc-skeleton template for two-Postgres separation verification; live-Railway commands runnable by Harry); E15-S2 Program.cs ShouldAutoMigrate helper + Database:AutoMigrate gate on both MigrateAsync sites (Testing branch exempt) + 4 unit tests; E15-S3 PostgresBackupService end-to-end refactor (6 docker-exec sites removed, direct pg_dump/pg_restore, AES-256-GCM encryption via new BackupEncryption.cs, RustFS upload via existing IAmazonS3, new PruneOldBackupsJob with 30-day retention) + new RegisterDailyBackupJob Hangfire helper with 03:00 + 04:00 UTC jobs gated !IsDevelopment() per ADR-020 inverse + Dockerfile postgresql-client-17 via PGDG + .env.example Backup__BucketName + doc Section 15 (~190 lines, 7 subsections); E15-S4 DevelopmentDataSeederGatingTests source-inspection regression guard (4 tests via Roslyn-style brace-counting against Program.cs source) + doc Section 16 (~210 lines, 7 subsections, browser-driven seeding procedure with 7 realm-role table + anti-patterns + KEYCLOAK_ADMIN recovery). **Both DEC-Needed blocks (E15-S3 hybrid-local-vs-RustFS + E15-S4 doc-target) auto-resolved to Option A under A41 escape** (user pre-declared autonomous mode + story recommendations + (a)/(b)/(c) recorded in Dev Agent Records). **Boundary code-review 9 patches APPLIED in same session**: P1 (Critical) pg_restore exit-code tightening to {0,1} (was >1 — silently accepted SIGKILL=-1); P2 (Critical) Section 15.4 + 15.5 Step 1 'aws s3' snippets replaced with RustFS web console + mc-in-container paths (backend Dockerfile does NOT install AWS CLI); P3 (High) 0-byte pg_dump file Failed-mark guard; P4 (High) PruneOldBackupsJob default-DateTime S3 object skip+warn (was: deleted as 'older than any cutoff'); P5 (High) PostgresBackupService MarkCompleted→MarkFailed restructured to single state transition; P6 (Medium) IsTruncated bool-vs-bool? cleanup (AWS SDK 3.7.x is bool); P7 (Medium) S3Objects null defensive guard; P8 (Medium) RegisterDailyBackupJobTests.RecurringJobIds_AreGloballyUnique pins all 6 project-wide job-ids as distinct; P9 (Low) DisablePayloadSigning documenting comment (required for RustFS S3-adapter compatibility; AES-GCM tag mitigates silent-tampering concern). **Quality gates**: dotnet build 0 warnings 0 errors; dotnet test 1442 + 158 + 410 = **2010 green** (baseline pre-E15 1976 + 34 new = 2010, zero regressions). **Cross-story orthogonal-AC parity (A31)**: toggle-default 3-anchor (E15-S2 appsettings.json + .env.example + helper default); backup-credential 3-anchor (Section 5.1 + Section 15.2 + Section 15.5 Step 2); 7-role parity 3-anchor (realm JSON + Section 16.3 + Roles.cs with mfa-required explicitly documented as conditional-flow trigger not in Roles.cs); connection-string parity 3-anchor (E15-S1 doc skeleton). **A28-A42 follow-through**: A28/A29/A30/A31/A34/A38/A40/A41/A42 all strongly applied; A33/A35/A36/A37/A39 N/A; A32 fully subsumed by A41 path B. **3 new action items A43-A45 promoted to project-context.md** under Story Authoring & Dev-Story Execution Rules: A43 (a)/(b)/(c) Debug Log template for A32/A41 Decision resolutions (verbatim quote of autonomous-mode directive + story recommendation + consequence chain); A44 Hangfire recurring-job-id global-uniqueness test (canonical anchor: RegisterDailyBackupJobTests.RecurringJobIds_AreGloballyUnique_AcrossAllRegisteredJobs); A45 A42 sixth category for documented-binary-surface reachability (every doc shell-command snippet's invoked binary must be reachable from the documented invocation site; verify against Dockerfile / image-build pipeline). **6 deferred items E15-FT-1..6** logged in deferred-work.md under 'code review of Epic-15 boundary (2026-06-02)' — PGDG GPG fingerprint pinning (E14-S1 or E19-S3 owner); DevelopmentDataSeederGatingTests on trimmed publishes; local-cache prune race; non-bool AutoMigrate env-var; Directory.CreateDirectory vs file-at-path; Section 11.2 cross-link verified. **3 dismisses**: PGDG key-rotation breakage (correct); single-file publish failure clear-error (acceptable); SDK bool→bool? upgrade (compile-time catch). **NOT addressed in this session — 38 `[!] needs-human-verify` items across the four stories' QG tables remain for Harry's live-Railway + browser-smoke execution**: live `railway service list` / `railway run psql` for E15-S1 verification, manual restore drill for E15-S3 AC-6, browser-Admin-Console 7-role + first-admin seeding for E15-S4 AC-2 + AC-3. These cannot be executed from this sandbox per LLM-dev-agent guardrails. **Sprint state**: E1-E3 + E9-E13 + E15 + E20 all done. NEXT per workflow.on_complete + project memory feedback_session_pacing_dev_cycles: commit this Epic-15 closure (S1-S4 + retro doc epic-15-retro-2026-06-02.md + project-context.md A43-A45 update + deferred-work.md E15-FT-1..6 + sprint-status update) as ONE coordinated commit before starting **Wave 8** in a fresh session. Wave 8 opens with E16 (Beta validation against live deploy) — E16-S3 (validate document upload against RustFS) is the natural opener since it exercises the same RustFS bucket-vs-bucket-vs-bucket configuration introduced by E15-S3 (Backup__BucketName=backups vs DocumentStorage__BucketName=iabconnect-documents distinct).) ## Previously: 2026-06-01 (E15 Wave-7 opener prep: bmad-create-story REFRESH for E15-S1 + E15-S3 — COMPLETE. **E15-S1** (Verify two-Postgres separation) refreshed from 19-line draft → ~440-line dev-ready spec. 11 ACs + 12 Tasks. Story authored as verification + documentation; doc deliverable = new Section 14 'Two-Postgres separation verification (E15-S1)' appended to docs/14_beta_railway_setup.md (6 subsections covering service inventory, credential distinctness, connection-string targets, schema isolation positive + negative checks, cross-credential rejection as migration-blast-radius proof, private-networking re-assertion). Key invariants per A31: connection-string parity 3-anchor for api ↔ doc Section 5.1 ↔ ADR-012 (same for keycloak ↔ Section 3.4); credential-distinctness re-runnable invariant; networking parity re-assertion of E13-S3 AC-2. Status backlog → ready-for-dev. **E15-S3** (Daily PostgreSQL backup to RustFS) refreshed from 19-line draft → ~510-line dev-ready spec. 10 ACs + 12 Tasks. **CRITICAL: E13-FT-6 docker-exec refactor folded into Task 0/1 as in-scope work** (not separate pre-story): existing PostgresBackupService.cs has 6 Process.Start('docker', ...) call sites incompatible with Railway runtime; refactor removes all 6 → direct pg_dump/pg_restore against connection-string-resolved host. Additional in-scope: backend Dockerfile adds postgresql-client-17 (~15-25 MB); new BackupEncryption.cs (System.Security.Cryptography.AesGcm 12-byte-nonce+16-byte-tag); new RegisterBackupJobs.cs with Hangfire 'daily-pg-backup' (03:00 UTC) + 'prune-old-backups' (04:00 UTC) gated on !IsDevelopment per ADR-020 inverse; new Backup__BucketName env var defaulting to 'backups'; Testcontainers Postgres + Minio round-trip tests. Doc deliverable = new Section 15 'Daily PostgreSQL backup + restore (E15-S3)' with 7 subsections including manual restore procedure + key-rotation runbook. Manual restore evidence on Beta as [!] Harry-task. Status backlog → ready-for-dev. **Cross-story dependency**: E15-S3 depends on E15-S1 done (verify Postgres separation) + Beta deploy live; E15-S2 stays backlog (depends on E15-S3 NOT — independent toggle work; could be picked up in parallel); E15-S4 stays backlog (Beta seeding depends on E15-S1 + E15-S3 patterns). **Sprint state:** E1-E3 + E9-E13 + E20 done. **E15 in-progress** (S1 + S3 ready-for-dev; S2 + S4 backlog). NEXT: Harry executes live Beta deploy per docs/14_beta_railway_setup.md (~60 [!] items) → then bmad-dev-story for E15-S1 (verification + Section 14 doc append) → then bmad-dev-story for E15-S3 (backend refactor + Hangfire + Section 15 doc append). Per project memory feedback_session_pacing_dev_cycles, recommend committing this large refresh + the entire E13 closure as one commit before starting any dev-story. **Note**: A41 Path B is now in effect for any future DEC-needed surfaces in autonomous mode.) ## Previously: 2026-06-01 (Epic-13 retrospective COMPLETE — Railway Beta Deployment closed end-to-end. Retro doc saved at _bmad-output/implementation-artifacts/epic-13-retro-2026-06-01.md. **5 new action items A38-A42 promoted to project-context.md**: A38 Doc-bundle pattern when N stories ship sections of one operator-facing document (write complete doc in first story; subsequent stories point at section anchors, no append); A39 Keycloak env-var docs MUST grep realm JSON for `${...}` placeholders + `\"bearerOnly\": true` clients (Epic-13 E1/E2/E3 patches generalized); A40 Recovery commands for tools not exercised in-session must be verified against tool docs OR marked `[!] verify before executing` (kc.sh bootstrap-admin patch P11 generalized); **A41 Path B chosen** — A32 explicit autonomous-mode escape clause: step (d) AskUserQuestion may be skipped when (1) user pre-declares autonomous-mode + (2) story file recommends an option + (3) dev-agent records (a)/(b)/(c) in Debug Log; A42 reread-as-a-stranger pass for documentation deliverables (cross-section contradictions, pre-filled placeholders, stale anchors, scope-leak — Blind Hunter finds these but pulling to story-close prevents accumulation). A28-A37 follow-through evaluated in retro §4: A28/A29/A30/A31/A34 strongly applied; A32 partial (resolved by A41 Path B); A33/A36/A37 N/A. Deferred-work E13-FT-10 (DEC-1 AskUserQuestion-skip soft flag) closed by A41 Path B. **Significant discoveries:** ZERO — E15 plan in epics-and-stories.md remains aligned. **One forward-defer affecting E15-S3 specifically (E13-FT-6)**: PostgresBackupService uses Process.Start(\"docker\", \"exec ... pg_dump ...\") incompatible with Railway runtime — E15-S3 spec MUST fold the docker-exec → direct-pg_dump refactor into Task 0 as prerequisite. Status transitions: epic-13-retrospective optional → done. **Sprint state at retro close:** E1-E3 + E9-E13 + E20 all done. E15 next (Wave 7 — Persistence + storage on Beta), gated on Harry's live Beta deploy (~60 [!] items per docs/14_beta_railway_setup.md). **NEXT per user retro choice:** bmad-create-story refresh for E15-S1 (verify two-Postgres separation) + E15-S3 (daily Postgres backup — must fold E13-FT-6 refactor). E15-S1 first as opener; E15-S3 next given the E13-FT-6 dependency. E15-S2/S4 stay backlog. Per A38 + A34 the refresh pass runs ahead of any dev-story.) ## Previously: 2026-06-01 (Epic-13 CLOSED — Railway Beta Deployment. Single continuous session per user 'no stopping, implement them full' directive delivered all 4 stories + epic-boundary code-review with 22 patches applied. Source artifacts: NEW docs/14_beta_railway_setup.md (~775-line consolidated Beta deployment doc-bundle covering all 4 stories — 13 sections: Prerequisites, Project creation, Service-by-service provisioning, GHA repo-variable population [chicken-and-egg resolved], Railway variables per service, Build-time vs runtime variables, Secret rotation, Networking topology, Health probes, First end-to-end deploy, Recovery procedures, Fork-replacement guidance, Reference tables); README.md cross-link under Option 4 'Beta deployment (Railway)' subsection; NEW frontend/src/app/api/health/route.ts (small Next.js Route Handler returning 200 JSON for Railway healthcheckPath of `web` service, ADR-017 compliant); NEW frontend/src/app/api/health/route.test.ts (2 Vitest contract tests). **DEC-1 = Option A auto-resolved** per user no-stopping directive — e17-s3-add-frontend-api-health-endpoint flipped backlog → done absorbed-by-e13-s4. Quality gates: dotnet not exercised (zero backend changes); npm typecheck 0 errors; npm lint 2 pre-existing baseline errors at members/segments/page.tsx unchanged + 0 new; vitest 137/137 green (baseline 135 + 2 new = 137). **Epic-boundary adversarial review** (3 layers — Blind Hunter + Edge Case Hunter + Acceptance Auditor) produced **22 patches APPLIED in same session**: P1 (E1) bearer-only `iabconnect-api` ClientSecret structurally unused — removed from §5.1 + §5.3 + §7; P2 (E2) realm-import `${IABCONNECT_BETA_HOST}` + `${FRONTEND_PUBLIC_URL}` placeholders missing from §5.3 — added with scheme-required rationale + Section 10.4 failure-mode breadcrumb; P3 (E3+H5) §6.3 parity check expanded from 3 to 5 anchors (added KC_HOSTNAME + KeycloakAdmin__BaseUrl) with concrete shell snippet + double-scheme footgun warning; P4 (H1) README 'immutable images + moving :beta tag' self-contradiction fixed; P5 (H2) §3.5 keycloak expected-state row contradicted §3.4 — rewrote; P6 (H4) §12 fork-replacement told forks to override NEXT_PUBLIC_SOURCE_URL via build-arg but workflow hard-codes it — clarified to edit the workflow file directly; P7 (H9) §11.2 'force-pull :beta re-seeds admin if database lost' misleading — rewrote with actual scope + realm-import side effects; P8 (H16) §1.1-1.4 Status checkboxes shipped pre-checked [x] — flipped to [ ] for reusability; P9 (E13) §5.1 file:line anchors drifted (121→139, 122→140) — updated; P10 (H8) §11.1 'lift RequireAdmin for debugging — revert before merge' footgun removed; railway shell in-container probe kept as safer path; P11 (E6) §11.2 `kc.sh bootstrap-admin` syntax wrong — rewrote with correct `bootstrap-admin user --password:env` Keycloak-26 subcommand + railway shell semantics; P12 (E8) §9.1 web healthcheck timeout 30s shorter than cold-start budget — bumped to 60s; P13 (E11) §8.3 external-reachability `psql` hangs on password prompt + DNS-fail vs slow-DNS indistinguishable — added `dig +short` first + `psql -w` + `PGPASSWORD=irrelevant`; P14 (E14) §1.2 NEXT_PUBLIC_FEEDBACK_URL exists in Dockerfile but not wired by build-args — added 'Not on this list' callout pointing at E18-S4; P15 (E15+H7) §10.4 step 3 'Admin role' is misleading (actual is lowercase `admin` per Roles.cs:16 + realm:167) + hard-coded harry@iabconnect.app — rewrote with realm-switch + lowercase admin + RequireAdmin anchor + `<your-admin-email>` placeholder; P16 (H10) §1.2+§1.4 gh-api user-vs-org ambiguous — added `gh api /users/<owner> --jq .type` probe + both-endpoint forms; P17 (H14) '5-line route' → 'small Route Handler' (file is 8 lines); P18 (H15) E17-S3 absorption scope-leak removed from operational §9.1 (lives correctly in sprint-status); P19 (H3) §3.3 RUSTFS no-rustfsadmin guard redundant — dropped; P20 (H12) route.test.ts toEqual → toMatchObject so additive observability fields don't break contract; P21 (E10) second `it` block missing status assertion — added; P22 (E4) §6.1 frontend Dockerfile doesn't declare BUILD_SHA/BUILD_DATE ARGs (Glob confirmed) — table updated to api-only + deferred-frontend-equivalent note. **Vitest re-run after P20+P21**: 2/2 green. **5 defers logged in deferred-work.md** under 'code review of Epic-13 boundary (2026-06-01)' as E13-FT-6..E13-FT-10: PostgresBackupService docker-exec incompatible with Railway (targets E15-S3); Frontend in-image HEALTHCHECK `/` traverses module-gating (targets E14-S3/E17-S4); KC_PROXY=edge deprecated in Keycloak 27.x (targets next KC bump); Vitest cleanup convention for non-React tests = A35 refinement (targets next retro); DEC-1 AskUserQuestion-skip A32 procedural soft-flag (targets E13 retro). **4 dismisses**: H6 PGPORT TCP-proxy speculation (Railway managed PG always exposes 5432 on private domain), H11 lowercase service-name CLI check (already covered by [!] Quality-Gates row), H17 README AGPL §13 silence (handled via CONTRIBUTING + LICENSE + /about + §12 fork-replacement), AC-8 'ok' vs \"ok\" quote-style (semantically equivalent + matches project Prettier convention). **Status transitions:** e13-s1/s2/s3/s4 review → done; epic-13 in-progress → done; e17-s3 backlog → done (absorbed-by-e13-s4 DEC-1=A). **NOT addressed in this session — 60+ `[!] needs-human-verify` Quality-Gates items per story remain for Harry's live Railway-dashboard + browser-smoke execution**: actual Railway project creation, six service provisioning, GHA repo-var population with real Railway hostnames, env-var population per service (Section 5), Public-Domain ON/OFF toggles (Section 8), healthcheckPath configuration, first end-to-end deploy + browser login round-trip. These cannot be executed from this sandbox per LLM-dev-agent guardrails. Sprint state: E1-E3 + E9-E12 + E20 + **E13 all done**. NEXT per workflow.on_complete + project memory `feedback_bmad_workflow`: bmad-retrospective for Epic-13 boundary (optional per sprint-status retrospective marker; recommended to capture A32 retro item + the 'consolidated doc-bundle vs per-story appended sections' authoring pattern that emerged this epic). Then **Wave 7 starts**: E15 (Persistence + storage on Beta) with E15-S1 (Verify two-Postgres separation) as the opener, dependent on a live Beta deploy from Harry's session.) ## Previously: 2026-06-01 (bmad-create-story BULK REFRESH for entire Epic-13 (Railway Beta Deployment): COMPLETE. All 4 stub files (e13-s1..s4, previously 19-line generate-beta-stubs.sh output) replaced with ~400-500 line dev-ready stories per A34 (bulk spec-refresh at epic start when N≥3 stories in one session). Refresh deltas vs. 2026-05-15 stubs: (1) e13-s1 grew to 11 ACs + 10 Tasks; concretized the six-service topology (3 public + 3 private — 6 services, NOT the 5 the epic-source AC-2 text says); enumerated the 4 BLOCKING [!] prerequisites Harry must complete before dev-story; resolved chicken-and-egg for `_BETA` GHA repo variables (Railway-assigned hostnames are the output of E13-S1 Task 2; Harry updates GHA variables between Task 2 and the next push); seeded the Keycloak JDBC env block in E13-S1 (NOT E13-S2) because Keycloak crash-loops without it. (2) e13-s2 grew to 11 ACs + 8 Tasks; full env-var enumeration per service (api ~30 vars, web ~5 runtime, keycloak ~10 KC_* + 3 realm-secret placeholders); classified build-time-baked vs runtime so dev-agent doesn't try to set NEXT_PUBLIC_API_URL on Railway; documented KEYCLOAK_ISSUER ≡ NEXT_PUBLIC_KEYCLOAK_ISSUER ≡ Keycloak__Authority 3-anchor parity invariant; documented three-way client-secret sharing (realm-import placeholder ↔ Keycloak DB ↔ consumer env var). (3) e13-s3 grew to 10 ACs + 9 Tasks; explicit curl + psql reachability tests from outside Railway; confirms Beta runs production-hardening branch (HSTS, HTTPS-redirect, strict CORS) per ADR-015; CORS hostile-origin preflight test. (4) e13-s4 grew to 9 ACs + 8 Tasks AND surfaces DEC-1 (Decision-Needed at dev-story start): web AC-2 healthcheckPath is `/api/health` but producer endpoint does NOT exist in frontend/ (verified `git ls-files frontend/src/app/api/health` = 0 files). Producer story E17-S3 is Wave 8 — 2 waves AFTER E13-S4 (Wave 6). 3 options surfaced in-story (A: in-line 5-line route here RECOMMENDED; B: pull E17-S3 forward; C: use `/` as healthcheck instead); per A32 dev-agent MUST surface via AskUserQuestion at Task 0.2. Cross-cutting findings: (a) ADR-012 graphic says 5 services but enumerates 6 — stories use 6 throughout; (b) Keycloak 26.5.2 + --optimized + KC_DB=postgres baked (E12-S3) means runtime needs KC_DB_URL/USERNAME/PASSWORD + KC_HOSTNAME + KC_PROXY=edge + KC_HTTP_ENABLED=true (ADR-016 + Wave-3 dockerfile do not enumerate these — e13-s2 AC-4 makes them explicit); (c) Beta runs HSTS + HTTPS-redirect + strict CORS per ADR-015 — e13-s3 Tasks 5/6 turn into live first-deploy verification; (d) A34 (bulk spec-refresh at epic start) is the EXACT pattern this session applied, validating the action item one epic later. Status transitions: epic-13 backlog → in-progress; e13-s1/s2/s3/s4 all backlog → ready-for-dev. Sprint state: E1-E3 + E9-E12 + E20 all done + E13 in-progress (4 ready-for-dev). NEXT per workflow.on_complete + project memory `feedback_session_pacing_dev_cycles`: commit this 4-story bulk-context refresh + sprint-status update and start bmad-dev-story for e13-s1 in a fresh session. IMPORTANT: Harry must complete 4 BLOCKING [!] preparation tasks first per e13-s1 prerequisite block — (1) DCO branch protection on main+beta; (2) 12 GHA repo variables populated (some values chicken-and-egg with E13-S1 Task 2); (3) first GHCR publish workflow run successful on `beta`; (4) flip 3 GHCR packages to Public visibility via GitHub UI. These are Harry-only and the dev-agent CANNOT execute them.) ## Previously: 2026-05-16 (bmad-dev-story for e12-s4-add-optional-docker-compose-full: COMPLETE — closes Epic-12 Wave-3 Dockerization. One new file at infra/docker-compose.full.yml (overlay extending base infra/docker-compose.yml: 4 new services [keycloak-full from E12-S3 Dockerfile + keycloak-full-realm-check one-shot health gate + api from E12-S1 Dockerfile + web from E12-S2 Dockerfile] + profile-based deactivation of base keycloak via `disabled-by-full` profile to free host port 8080). One README edit adding `Option 4: Local Beta-shape testing (full overlay)` subsection right after the existing `Option 3: Container images (Beta-shape)` block (README:363). **Zero source-code changes** — no backend, frontend, SPI, dev realm, or sanitized realm edits. Quality gates: docker compose config base+merged both green (9 services in merged set, NOT 8 as AC-8 typo'd — its own enumeration listed 7 long-running + 2 one-shot) · docker compose build green for all 3 images (iabc-api:local, iabc-web:local, iabc-keycloak:local) · docker compose up healthy 7 long-running + 2 Exited(0) one-shot + 1 pre-existing seq Restarting loop (unrelated to overlay; we made zero edits to seq) · AC-9 smoke 3/4 HTTP 200 (web `/`, keycloak `/realms/iabconnect/.well-known/openid-configuration` 6612B, api `/health/ready`) + 1 RustFS reachability proof via HTTP 403 GET / 501 HEAD with valid x-request-id headers (RustFS policy responses, not connection failures — AC intent satisfied) · teardown clean (9 containers removed; 4 of 5 named volumes deleted, `infra_minio_data` survives as pre-existing pre-RustFS-migration artifact). **Story-vs-Reality deviation the reviewer MUST see:** ASPNETCORE_ENVIRONMENT set to `Development` not `Beta` per AC-2 because backend hardcodes `RequireHttpsMetadata=true` for non-Dev/non-Testing env at DependencyInjection.cs:134 — local Keycloak runs HTTP only (no TLS), so any non-Dev/non-Testing env crashes AuthenticationMiddleware init on EVERY request (including unauthenticated /health) with `System.InvalidOperationException: The MetadataAddress or Authority must use HTTPS unless disabled for development by setting RequireHttpsMetadata=false.` There is NO `Keycloak__RequireHttpsMetadata` config key to defeat this via env-var override. YAML carries verbose comment + follow-up pointer for E14-S2 / backend config-refactor story to surface RequireHttpsMetadata as a config key. The 'Beta-shape' intent of the overlay is preserved by: Railway-mirroring port mapping (api 5000:8080, web 3000:3000), frontend BETA banner via NEXT_PUBLIC_ENV_LABEL=beta build-arg, container topology, RetentionEnforcement disabled. Other minor deviations: AC-9 /health/live -> /health/ready (backend exposes /health and /health/ready only per DependencyInjection.cs:330-331; AC-9 itself permits 'or whatever the backend exposes'); AC-8 'all 8 services' is a typo (real count is 9; AC-8's own enumeration listed 7 long-running + 2 one-shot). Story Questions resolved: Q1 health endpoint missing confirmed; Q2 compose v5.0.2 way past v2.20+ required; Q3 single Postgres for api+Keycloak works (no DDL conflict); Q4 login round-trip Task 8 marked [!] (needs human browser + manual tester user creation in Keycloak Admin Console). Status transitions: e12-s4 in-progress -> review. **Epic-12 boundary reached:** ALL Epic-12 stories now in `review` (e12-s1, e12-s2, e12-s3, e12-s4). NEXT per workflow.on_complete hybrid policy: bmad-code-review for the Epic-12 boundary diff (adversarial review of all 4 stories: backend Dockerfile + frontend Dockerfile + custom Keycloak image + docker-compose.full.yml overlay), then bmad-retrospective. After retrospective the Wave-3 Dockerization epic flips to `done` and Wave-4 starts (E20-S3 backend /about endpoint + E20-S4 frontend license footer — both need Wave-3 build-args). Per project memory `feedback_session_pacing_dev_cycles`, recommend committing this dev-story state (Epic-12 close-ready) before starting bmad-code-review in a fresh session — code-review is its own heavy session. Per project memory `feedback_bmad_workflow` (hybrid: CR+ER at epic boundary), bmad-code-review now appropriately fires for the Epic-12 boundary diff.) ## Previously: bmad-dev-story for e12-s3-add-custom-keycloak-image-with-spi: COMPLETE. Two new files: infra/keycloak/Dockerfile (two-stage maven:3.9-eclipse-temurin-17 spi-build -> quay.io/keycloak/keycloak:26.5.2 kc-final; ARG KC_DB=postgres build-time parameter; 5 OCI labels; CMD start --optimized) and infra/keycloak/realms-beta/iabconnect-realm.json (sanitized realm — surgical edits to a copy of the dev realm: 2 client secrets swapped to ${IABCONNECT_*_CLIENT_SECRET} placeholders, iabconnect-frontend redirectUris/webOrigins updated to Railway+${FRONTEND_PUBLIC_URL} pattern dropping localhost:3000, 6 dev seed users with passwords removed). **DEVIATION from Task 1.3 the reviewer should see:** kept the single `service-account-iabconnect-admin` user — no credentials, just realm-management:realm-admin role mapping that gives the iabconnect-admin confidential client its admin-API privileges (used by KeycloakAdminService.cs:403). Removing it would silently break backend admin ops on Beta with no security benefit. AC-5's narrower wording (\"6 dev seed users\") was followed in preference to Task 1.3's broader \"users: []\" (the 7th user is service-account, not a seed user). **Three story Questions resolved + one new constraint discovered:** Q1 redirect URIs picked the conservative Railway+${FRONTEND_PUBLIC_URL} default dropping iabconnect.ch (production isn't on Beta gate); Q2 ${VAR} substitution for client.secret confirmed working in 26.5.2 by non-crashing smoke boot; Q3 realm-import idempotency confirmed via --import-realm flag on CMD args; Q4 FALSIFIED: --features= is for built-in Keycloak features only — custom SPI provider IDs are auto-discovered from /opt/keycloak/providers/. First build attempt failed with `'disable-new-users' is an unrecognized feature`; fixed by dropping the --features= flag. **NEW constraint:** KC_DB is build-time-frozen under --optimized (Quarkus augments classes per driver). First smoke run with -e KC_DB=dev-file rejected with `kc.db build-time options differ from what is persisted`. Resolution: parameterize Dockerfile with `ARG KC_DB=postgres` (production default matches E13-S2 Beta) and a separate `:smoke` tag built with `--build-arg KC_DB=dev-file` for local non-postgres smoke. Production image stays Beta-shape. Quality gates: docker build green 629 MB (target <=800) · smoke boot 10.515s (target <30) emits the `Keycloak 26.5.2 on JVM ... started in 10.515s` line · curl http://localhost:8090/realms/iabconnect/.well-known/openid-configuration => HTTP 200 6612B with parsed issuer/token_endpoint matching expectations · SPI registration confirmed via 3 independent evidence sources (build-time KC-SERVICES0047 log line, runtime ls /opt/keycloak/providers/ shows disable-new-users-spi.jar, successful realm import — Keycloak would refuse a realm referencing an unregistered eventsListener) · AC-14 no-secrets grep CLEAN (MSYS_NO_PATHCONV=1 prefix needed for Git-Bash path conversion) · AC-15 local-dev compose up postgres+keycloak still serves HTTP 200 on port 8080 (zero edits to docker-compose.yml, realms/iabconnect-realm.json, or providers/disable-new-users/*). Task 9 (manual browser round-trip via full stack) marked [!] per A30 — needs E12-S4's docker-compose.full.yml to orchestrate backend+frontend+custom-keycloak and a Beta tester account created post-boot via Admin Console; dev-agent non-interactivity scope. Status transitions: e12-s3 in-progress -> review. Sprint state: E20 in-progress (5 ready-for-dev) + E12 in-progress (3 review + 1 ready-for-dev). NEXT: per workflow.on_complete hybrid policy, Epic-12 boundary needs e12-s4 — recommend bmad-dev-story for e12-s4-add-optional-docker-compose-full (Wave-3 close + unblocks Task 9 manual verify for e12-s1/s2/s3). Per project memory `feedback_session_pacing_dev_cycles`, recommend committing this dev-story state and starting the e12-s4 dev-story in a fresh session.) ## Previously: bmad-dev-story for e12-s2-add-frontend-dockerfile-standalone: COMPLETE. Three new files at frontend/ — Dockerfile (multi-stage deps -> build -> runtime; node:22-alpine throughout; all 9 NEXT_PUBLIC_* declared as BOTH ARG and ENV per AC-4; HOSTNAME=0.0.0.0 mandatory; USER node UID 1000; OCI labels minus per-build revision/created which E20-S5 injects at GHCR publish time), .dockerignore (8 category groups; source of truth since no frontend/.gitignore exists), public/.gitkeep (zero-byte placeholder — repo had no frontend/public/ but spec assumed standard Next.js layout; gitkeep makes AC-7 third COPY work and establishes the dir for future static assets). One README edit: generalized E12-S1's 'Option 3 Backend container image' section into 'Option 3 Container images' with both backend + frontend build commands. **Zero frontend/src/** changes.** Quality gates: typecheck exit 0 · lint 2 baseline-errors (no new) · vitest 127/127 green (E11-S3 baseline match, story adds zero tests as specified) · npm run build green via Task-3 spike. Docker evidence: dev-shape build 243 MB (target <=250 MB), beta-shape build green, both AC-14 bake-evidence greps returned matches (api.example.app baked into /app/.next/static/chunks/00eb41ece76a42fb.js as literal 'let t=\"https://api.example.app\"' AND docs.example.app baked into /app/.next/required-server-files.json AND /app/server.js env-block). Runtime smoke captured the critical `Network: http://0.0.0.0:3000` stdout line proving HOSTNAME override; curl returned HTTP 200 with 108118-byte body. Non-root verified via `docker exec ... id`: uid=1000(node). Task 10 fork-SOURCE_URL grep returned a match at /app/.next/static/chunks/3a532a83502e6991.js — bake-half done, visual-half stays [!] until E20-S4 lands the footer. **Two deviations the reviewer should know about:** (1) RUN `npm install -g npm@11` added to deps stage — node:22-alpine ships npm 10.9.8 but package-lock.json is npm-11-authored, npm 10's stricter lockfile parser reports `Missing: @swc/helpers@0.5.21 from lock file` even though the same `npm ci` passes on host npm 11.6.0; the in-image pin is the most reproducible fix; (2) frontend/public/.gitkeep added because the repo has no public/ directory and the AC-7 COPY would otherwise fail. A28 spike caught both of these as planned. Status transitions: e12-s2 in-progress -> review. Sprint state: E20 in-progress (5 ready-for-dev) + E12 in-progress (2 review + 2 ready-for-dev). NEXT: per workflow.on_complete hybrid policy, only e12-s1 + e12-s2 are review-ready -- Epic-12 boundary still needs e12-s3 + e12-s4. Recommend bmad-dev-story for e12-s3 (custom Keycloak SPI image, Wave-3 sibling). Per project memory `feedback_session_pacing_dev_cycles`, commit this dev-story state and start the next dev-story in a fresh session.) ## Previously: bmad-dev-story for e12-s1-add-backend-dockerfile-multistage: COMPLETE. Backend Dockerfile (multi-stage .NET 10, Europe/Zurich tzdata, USER 1000 non-root, AGPL-3.0 OCI labels, BUILD_SHA/BUILD_DATE build-args defaulting to 'unknown') + backend/.dockerignore landed at backend/Dockerfile and backend/.dockerignore. **rustfsadmin Beta-blocker resolved** via paired edit on base appsettings.json:37-43 AND DocumentStorageSettings.cs:10-14 class-initializers — `strings src/IabConnect.Infrastructure/bin/Debug/net10.0/IabConnect.Infrastructure.dll | grep -i rustfsadmin` returns CLEAN; only appsettings.Development.json retains the dev literals (loads only when ASPNETCORE_ENVIRONMENT=Development, per AC-8). **Test-infrastructure timing seam at Infrastructure/DependencyInjection.cs:259 resolved** by env-var injection in TestWebApplicationFactory's static constructor — env vars are part of the default config chain set up by WebApplication.CreateBuilder at Program.cs:17, so they propagate to the eager `.Get<DocumentStorageSettings>()` read at Program.cs:32 BEFORE WebApplicationFactory's ConfigureAppConfiguration callbacks run at builder.Build() (Program.cs:34). Resolves the previously-deferred cleanup note at AppSettingsLayeringTests.cs:57-65 without touching the production DI registration (path 1 'IOptions refactor' explicitly out of scope per the story). AppSettingsLayeringTests extended with 6 new theory rows (3 DocumentStorage:* keys × 2 theories) guarding the AC-7 invariant for base AND Beta-layered config. **Two spec-vs-reality observations for reviewer:** (a) base image is Ubuntu 24.04 not Debian-bookworm — Microsoft moved aspnet:10.0 to Ubuntu Noble; ICU and tzdata both pre-bundled (apt-get install tzdata is now ~14B defense-in-depth); (b) image size 384 MB > 350 MB story target — root cause is untrimmed framework-dependent publish (154 MB vs 30 MB story estimate) due to Directory.Build.props Release config `PublishTrimmed=false` and the wide package profile (EF, Hangfire, Serilog, QuestPDF, MediatR, AWS S3); trimming/AOT optimization out of scope. Task 1.5 (interactive `dotnet run` against local infra/docker-compose.yml stack) flagged `[!]` per project-context A30. Task 10 (build-arg injection) executed directly as `[x]` — `docker build --build-arg` + `docker inspect` is non-interactive within dev-agent scope. Quality gates: dotnet build 0 warnings · dotnet test 1963/1963 green (baseline 1957 + 6 new theory rows) · docker build green in 65s · docker run emits bootstrap Serilog line BEFORE the expected Npgsql connection-refused crash · docker build --build-arg round-trips BUILD_SHA/BUILD_DATE into env. Status transitions: e12-s1 in-progress -> review. Sprint state: E20 in-progress (5 ready-for-dev) + E12 in-progress (1 review + 3 ready-for-dev). NEXT: per workflow.on_complete hybrid policy, only e12-s1 of Epic-12 is review-ready so far -- recommend bmad-dev-story for the next ready-for-dev story in sprint order (e20-s1 by top-to-bottom or e12-s2 to keep Wave-3 momentum). Per project memory `feedback_session_pacing_dev_cycles`, recommend committing this dev-story state and starting the follow-up in a fresh session.) ## Previously: bmad-create-story for entire Epic E12: COMPLETE. Comprehensive context files authored for e12-s2 (Frontend Dockerfile), e12-s3 (Custom Keycloak image with SPI), e12-s4 (docker-compose.full.yml overlay) -- all replace 20-line stubs with ~400-500 line dev-ready stories matching the e11-s3 / e12-s1 depth standard. **E12-S2 highlights:** three-stage Dockerfile (deps -> build -> runtime), node:22-alpine throughout, ALL 9 NEXT_PUBLIC_* vars declared as BOTH ARG and ENV (critical: ENV alone makes Next.js silently inline empty strings), `HOSTNAME=0.0.0.0` mandatory in runtime stage (the #1 silent-failure mode for Next.js standalone in Docker), `--chown=node:node` on the three runtime COPYs, standalone shape verified via E11-S3's outputFileTracingRoot. **E12-S3 highlights:** two-stage Dockerfile (maven:3.9-eclipse-temurin-17 spi-build -> quay.io/keycloak/keycloak:26.5.2), separate sanitized realm at new `infra/keycloak/realms-beta/iabconnect-realm.json` (preserves dev realm unchanged), AC-5 explicit sanitization spec (drop all 6 dev seed users, swap committed dev secrets at lines 228+252 for `${IABCONNECT_*_CLIENT_SECRET}` placeholders, update redirect URIs for Railway domain pattern), pre-build with `kc.sh build --features=disable-new-users` so runtime uses `start --optimized` (sub-15s start vs 30-60s without), AC-14 strings-grep verification that no committed secrets ship in the image, local-dev workflow unchanged (volume-mount path retained). **E12-S4 highlights:** overlay file (not replacement), 3 new app services (api/web/keycloak-full), base `keycloak` service deactivated via `profiles: [\"disabled-by-full\"]` to free host port 8080, one-shot `keycloak-full-realm-check` curl-poll service as realm-import health gate, explicit annotation of localhost-vs-compose-hostname rule per env var (NEXT_PUBLIC_* use localhost because browser-resolved; server-side env vars use compose hostnames), local-dev secrets reused directly (not env-indirected -- they are local convenience, NOT real Beta secrets), HARD upstream gate (Task 0 spike verifies all 3 Dockerfiles exist before authoring). All 3 stories carry project-context A28-A30: Task 0 spike-first, AC-Subitem Completion Check, [!] manual-verify markers for browser-interactive flows. Status transitions: e12-s2 backlog->ready-for-dev; e12-s3 backlog->ready-for-dev; e12-s4 backlog->ready-for-dev. Sprint state: E20 in-progress (5 ready-for-dev) + E12 in-progress (4 ready-for-dev, 0 backlog -- entire epic context-ready). NEXT: bmad-dev-story for e12-s1 to start Wave-3 implementation. e12-s4 has HARD upstream gate -- its Task 0 will HALT until e12-s1, e12-s2, e12-s3 Dockerfiles all land. Per project memory `feedback_session_pacing_dev_cycles`, recommend committing this front-loaded context + sprint-status update and starting dev-story in a fresh session.) ## Previously: bmad-create-story for e12-s1-add-backend-dockerfile-multistage: COMPLETE. Comprehensive story file authored at _bmad-output/implementation-artifacts/e12-s1-add-backend-dockerfile-multistage.md (replaces the 167-line pre-pivot stub). 14 ACs (BDD-shaped, file:line anchored), 11 tasks (Task-0 spike + Task-1 paired edit + Task-2 .dockerignore + Task-3 Dockerfile + Tasks 4-7 verification + Task-8 README + Task-9 quality gates + Task-10 [!] manual Beta-shape build-arg). Three corrections versus the pre-pivot stub: (a) SDK/runtime tags 9.0->10.0 (global.json pins 10.0.102; Directory.Build.props targets net10.0); (b) explicit `/p:PublishSingleFile=false` override in `dotnet publish` to neutralize the Release-config-default at Directory.Build.props:14 that would otherwise produce a single-file binary incompatible with `ENTRYPOINT [\"dotnet\",\"IabConnect.Api.dll\"]`; (c) rustfsadmin Beta-blocker resolved as a paired edit -- AC-7.1 strips credentials from appsettings.json:37-43, AC-7.2 strips matching class initializers from DocumentStorageSettings.cs:10-14 to take the strings out of compiled IL (verified by Task 1.6 `strings *.dll | grep rustfsadmin` -> CLEAN). Lazy-init via IAmazonS3 singleton factory at DependencyInjection.cs:261-270 means missing Railway env vars surface as a 500 on first /api/documents/... call rather than a boot crash -- documented as the intended fail mode. Epic-11 retro A28/A29/A30 woven in: Task 0 spike-first for the (deceptively) low-risk JSON edit; Task 9.5 AC-Subitem Completion Check at story close; Task 10 [!] marker for non-interactive manual verification. Status transitions: epic-12 backlog->in-progress; e12-s1 backlog->ready-for-dev. Sprint state: E20 in-progress (5 ready-for-dev) + E12 in-progress (1 ready-for-dev, 3 backlog). NEXT: bmad-dev-story for e12-s1, OR bmad-create-story for e12-s2 (frontend Dockerfile, Wave-3 sibling) if user wants parallel context for the next dev session. Per project memory `feedback_session_pacing_dev_cycles` user prefers commit-then-fresh after heavy review sessions -- recommend committing this context file + sprint-status update then starting dev-story in a fresh conversation.) ## Previously: bmad-retrospective for Epic E11: COMPLETE. Retro doc saved at epic-11-retro-2026-05-16.md. 3 new action items A28 (Spike-First for low-risk-mechanical cleanup specs) / A29 (AC-Subitem Completion Check at Story Close) / A30 (Three-State Task Checkbox [x]/[!]/[ ] for Manual-Verify ACs) — all promoted to project-context.md as Rules. A27 from Epic 10 retro carried forward (dev-API restart docs). E10 follow-through: A24 N/A (no interim patches in E11), A25 Applied (no patch demotions), A26 Applied (D4 fail-mode companion-surface evaluation), A27 Not Addressed (carry forward to E12-S1). E11 Beta-blocker: rustfsadmin literals in base appsettings.json — must resolve in E12-S1 (IOptions refactor OR image-side .dockerignore route, decision deferred to bmad-create-story for e12-s1). Sprint state: epic-11 done + epic-11-retrospective done (this update). NEXT: per workflow.on_complete hybrid policy, bmad-create-story for e12-s1-add-backend-dockerfile-multistage — Wave 3 start. The first E12 story scope must explicitly decide rustfsadmin disposition.) ## Previously: bmad-code-review for Epic E11 boundary: COMPLETE. Adversarial review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) over full E11 diff (S1+S2+S3, ~2772 lines) produced 4 decisions + 6 patches + 7 defers + ~17 dismisses. All 4 decisions resolved: D1 + D2 -> defer (rustfsadmin pre-Beta follow-up via IOptions refactor; AC-9 test gap blocked by E15-S2 Database__AutoMigrate); D3 + D4 -> patch (BetaBanner case-insensitive envLabel?.trim().toLowerCase() === 'beta'; RegisterRetentionEnforcementJob orphan-RemoveIfExists branch). 6 patches applied: E11-S2 BetaBanner (case-insensitive, sessionStorage try/catch for Safari Private Browsing, lazy useState init to kill flash-of-orange flicker, fork-friendly feedback URL via NEXT_PUBLIC_SOURCE_URL fallback), NEXT_PUBLIC_FEEDBACK_URL documented in frontend/.env.example, RegisterRetentionEnforcementJob else-RemoveIfExists branch; E11-S3 parseDocumentHost rejects path/query/fragment/userinfo/IPv6 with cited offending field and wraps URL constructor errors with env-var-name breadcrumb. Test deltas: backend 1957/1957 stays green (1 test renamed Register_DoesNothing -> Register_RemovesJob); frontend Vitest 113 -> 127 across 17 files (+14 net: +7 parser hardening cases, +7 BetaBanner cases). Smoke build green with all 3 Beta env vars baked. 7 defers logged in deferred-work.md under 'code review of Epic E11 boundary (2026-05-16)' section: BetaBanner test semantics (build-time vs runtime), appsettings.Beta.json Logging.LogLevel duplication, empty Authority/Smtp slow runtime failures, AppSettingsLayeringTests deferred-key coverage, E11-S2 Task 11 process smell, outputFileTracingRoot scope-add disclosure, .env.example line-number anchor rot. Status: e11-s1/s2/s3 all done; epic-11 in-progress -> done. NEXT: bmad-retrospective for Epic-11 close, then bmad-create-story for Wave-3 (E12-S1 Backend Dockerfile, E12-S2 Frontend Dockerfile, E12-S3 Custom Keycloak SPI image).)"
# --- history ---------------------------------------------------------------
# 2026-05-13: Epic-Close-Gate after clearing all 11 [Review][Patch] findings per
# sprint-change-proposal-2026-05-13.md. E1.S1-S4 flipped review -> done.
# E1.S5 deferred -- pending provider credentials/scopes/whitelist decisions.
# 2026-05-13: E2.S1 re-contextualized (initial 2026-05-12 file was a generic
# template). New version has concrete 8 ACs, file paths with line numbers,
# Epic-1 retro action items A1/A2/A4 wired in, phone-normalization Option B
# decided, tests at Application+Infrastructure+API layers.
# 2026-05-13 (later): Epic-1 closed as done with S5 carried as 'deferred'.
# 2026-05-13 (E2.S1 close): E2.S1 implementation complete; 1485/1485 backend green.
# 2026-05-14 (correct-course): MAJOR scope change approved -- Generic Organization
# Platform & Module Configuration. epic-9 + epic-10 added. Proposal:
# planning-artifacts/sprint-change-proposal-2026-05-14.md.
# 2026-05-14 (re-prioritization): epics 4-8 reset in-progress -> backlog (stories
# ready-for-dev -> backlog) per user decision. epic-9 + epic-10 are now the active
# focus and preempt 4-8. Resolves OD-3.
# 2026-05-14 (Epic-3 close): Epic-3 retrospective COMPLETE + Epic-3 closed; §9
# cleanup sprint COMPLETE. Backend 1837/1837 green, 0 warnings.
# 2026-05-14 (downstream re-alignment): bmad-edit-prd -> bmad-create-architecture ->
# bmad-create-ux-design -> bmad-create-epics-and-stories all re-run for the pivot;
# E9/E10 now fully specified in epics-and-stories.md (REQ-086/REQ-087).
# 2026-05-14 (readiness): bmad-check-implementation-readiness re-run against the
# re-aligned artifacts -> READY (0 critical, 0 major, 9 minor). Report:
# planning-artifacts/implementation-readiness-report-2026-05-14.md.
# 2026-05-14 (sprint re-planning): E9/E10 placeholder keys replaced with
# canonical keys from the epic file; block reordered to execution priority.
# 2026-05-14 (bmad-code-review): epic-boundary review over the full E10 diff.
# 4 decision-needed (resolved -> 3 patch + 1 dismissed), 13 patch, 4 defer,
# 13 dismissed. User left patches as action items -> e10-s1..s5 review ->
# in-progress. Findings in per-story Review Findings sections + deferred-work.md.
# 2026-05-14 (bmad-dev-story): all 13 [Review][Patch] findings applied across
# e10-s1..s5. Backend 1936/1936 green 0 warnings; frontend typecheck + lint
# green, Vitest 92/92. e10-s1..s5 in-progress -> review. NEXT: bmad-code-review
# epic-boundary re-review.
# 2026-05-15 (bmad-code-review): Epic-10 boundary RE-REVIEW closed. 4 decisions
# (DN-1..DN-4) resolved to patches, 16/17 patches applied as code (P-S4-4
# demoted to defer — Sidebar submenu test untestable without refactor), 10
# defers logged (2 cross-cutting in deferred-work.md, rest per-story). Backend
# 1942/1942 green (+6 new tests), frontend Vitest 89/89 green. e10-s1..s5
# review -> done; epic-10 in-progress -> done. NEXT: bmad-retrospective for
# Epic-10 close, then E4-E8 backlog resumes (E10 ships first so the external
# API in E8 inherits module enforcement from day one).
# 2026-05-15 (bmad-retrospective): Epic-10 closed via epic-10-retro-2026-05-15.md.
# 4 new action items A24-A27 (interim-patch labeling, test-gap testability triage
# check, companion-surface fail-mode patching, dev-API restart documentation).
# Epic-9 A19-A23 follow-through evaluated. epic-10-retrospective optional -> done.
# PIVOT WEDGE COMPLETE: E9 + E10 shipped. No epic currently in-progress; E4-E8
# all backlog. NEXT: bmad-sprint-planning to re-prioritize / re-validate E4-E8
# readiness after the 2026-05-14 pivot (pre-pivot story files may have spec
# drift — branding strings + module-less assumptions); then bmad-create-story
# for that epic's first ready-for-dev story.
# 2026-05-15 (SCP-2026-05-15 Beta-on-Railway and Open Source Foundation pivot):
# bmad-edit-prd -> bmad-create-architecture -> bmad-create-epics-and-stories ->
# bmad-create-ux-design all re-run against SCP-2026-05-15. PRD merged REQ-088
# (Beta Deployment Readiness) and REQ-089 (Open Source License Surface) as
# PRD-native, added Product Principles 10 (OSS by Default) and 11 (Deployment
# Portability), Beta Environment Operations NFR group, Beta+OSS Acceptance
# Criteria section, restructured Recommended Roadmap to make E11-E20 the active
# sprint focus and demote E4-E8 to Deferred Backlog, recorded OD-6 (status-doc
# out of sync with closed epics — separate task). Architecture appended
# ADR-009..021 (AGPL, DCO, Railway, Topology, RustFS+volume, GHCR, Beta env
# strategy, custom Keycloak image, container logging/health, Mailtrap, backup
# destination, retention suppression, AGPL §13 source-disclosure). Epics+stories
# appended E11-E20 with 40 new stories all carrying observable ACs, ADR
# references, and tests/evidence. UX-design appended 4 Beta+OSS Flow Specs
# (BETA Banner, License Footer, About Info Display, Feedback Channel) plus
# Navigation Model and Permission Rule updates. 40 story stubs committed to
# disk (e11-s1..e20-s5) plus generate-beta-stubs.sh.
# 2026-05-15 (bmad-validate-prd Round 2): PRD re-validated after the simple
# fixes and downstream alignment. Overall Pass clean. Holistic Quality 5/5
# Excellent (up from 4/5 in Round 1). All Round-1 Top-3 Improvements closed:
# Success Metrics gap for REQ-088/089 closed, Mailtrap FR/AC inconsistency
# closed, downstream artifact alignment complete. Report: planning-artifacts/
# prd-validation-report-2026-05-15-round2.md.
# 2026-05-15 (bmad-check-implementation-readiness): READY verdict. 0 critical,
# 0 major, 4 minor/informational (operational epics framing, story-stub drift
# check pending, OD-6 doc-sync, ADR style mix — all non-blocking). Report:
# planning-artifacts/implementation-readiness-report-2026-05-15.md.
# 2026-05-15 (bmad-sprint-planning, this update): added 10 new epics, 40 new
# stories, 10 new retrospectives. Wave-aware status: E20-S1 and E20-S2 promoted
# to ready-for-dev (Wave 1 of SCP-2026-05-15 §6); all other E11-E20 stubs stay
# backlog held by wave order. Block reordered to execution priority.
# 2026-05-15 (bmad-create-story for ALL of Epic-20): re-created comprehensive
# context for e20-s1 (was 112-line draft; now 175+ lines with explicit
# task/AC/file-list/test-evidence cross-references) and filled out the four
# stubs e20-s2..e20-s5 to full dev-ready context (180-240 lines each). User
# explicitly overrode the SCP-2026-05-15 §6 wave gate — all five E20 stories
# are now ready-for-dev so dev work can run in parallel with the E11/E12
# upstreams. epic-20 status backlog -> in-progress. CAVEATS the dev-story
# workflow must surface: E20-S3 needs BUILD_SHA/BUILD_DATE env vars that
# arrive via E12-S1's backend Dockerfile (story degrades to 'unknown'
# strings until E12 lands — verified by the AC-9 in-memory config test);
# E20-S4 references NEXT_PUBLIC_SOURCE_URL that E12-S2 also propagates as
# a build-arg; E20-S5 has HARD deps on E12-S1/S2/S3 Dockerfiles existing
# on disk and will block at Task 1 if any are missing. The dev agent must
# escalate Dockerfile absence rather than fabricate a workaround. NEXT:
# bmad-dev-story for e20-s1 (LICENSE/NOTICE/CONTRIBUTING/DCO — zero
# upstream deps, fully unblocked).
# ---------------------------------------------------------------------------
project: iab-connect
project_key: NOKEY
tracking_system: file-system
story_location: "_bmad-output/implementation-artifacts"
epics_source: "_bmad-output/planning-artifacts/epics-and-stories.md"
readiness_report: "_bmad-output/planning-artifacts/implementation-readiness-report-2026-05-15.md"

development_status:
  # === DONE — pre-pivot delivery (E1-E3) ===================================
  epic-1: done
  e1-s1-configure-role-based-mfa-policy: done
  e1-s2-add-admin-mfa-support-operations: done
  e1-s3-add-session-and-device-visibility: done
  e1-s4-add-session-revocation: done
  e1-s5-add-social-and-enterprise-identity-providers: deferred
  epic-1-retrospective: done

  epic-2: done
  e2-s1-add-duplicate-candidate-detection: done
  e2-s2-show-duplicate-warnings-in-member-create-edit: done
  e2-s3-implement-safe-member-merge: done
  e2-s4-add-duplicate-review-ui: done
  epic-2-retrospective: done

  epic-3: done
  e3-s1-add-event-check-in-roster-and-export: done
  e3-s2-add-qr-and-manual-check-in-flow: done
  e3-s3-add-volunteer-planning-domain-and-api: done
  e3-s4-add-volunteer-planning-ui-and-reminders: done
  e3-s5-add-calendar-feed-and-ics-export: done
  epic-3-retrospective: done

  # === DONE — generic white-label pivot (E9, E10) ==========================
  epic-9: done
  e9-s1-extend-systemsettings-and-add-branding-admin-ui: done
  e9-s2-replace-hardcoded-organization-references-in-frontend: done
  e9-s3-replace-hardcoded-organization-references-in-backend: done
  e9-s4-generalize-i18n-branding-strings: done
  epic-9-retrospective: done

  epic-10: done
  e10-s1-add-module-settings-data-model-and-service: done
  e10-s2-add-module-settings-api-and-modules-admin-tab: done
  e10-s3-add-backend-module-enforcement: done
  e10-s4-add-frontend-module-enforcement: done
  e10-s5-add-public-view-toggle-and-cross-module-dependency-handling: done
  epic-10-retrospective: done

  # === ACTIVE FOCUS — Beta-on-Railway and Open Source Foundation pivot ====
  # SCP-2026-05-15 introduces 10 new epics (E11-E20) covering REQ-088 (Beta
  # Deployment Readiness, E11-E19) and REQ-089 (Open Source License Surface,
  # E20). Wave order from SCP-2026-05-15 §6:
  #   Wave 1: E20-S1, E20-S2 (LICENSE/DCO + SPDX policy — dependency-free)
  #   Wave 2: E11-S1, E11-S2, E11-S3 (configuration hygiene)
  #   Wave 3: E12-S1, E12-S2, E12-S3 (containerization)
  #   Wave 4: E20-S3, E20-S4 (source-disclosure /about + footer — needs Wave 3 build-args)
  #   Wave 5: E20-S5 (CI publish to GHCR — needs Wave 3 Dockerfiles)
  #   Wave 6: E13-S1..S4 (Railway provisioning — needs Wave 5 images)
  #   Wave 7: E15-S1..S4, E16-S3 (persistence + storage on Beta)
  #   Wave 8: E14-S1..S5, E17-S1..S4 (security + observability)
  #   Wave 9: E18-S1..S4 (Beta tester operations)
  #   Wave 10: E19-S1..S4 (production prep — NOT a Beta blocker)
  #
  # Status convention: file presence does NOT auto-promote to ready-for-dev.
  # Only Wave 1 stories (E20-S1, E20-S2) are ready-for-dev now. Promote the
  # next wave's stories to ready-for-dev when starting that wave.
  #
  # Cross-epic dependencies (epics-and-stories.md Dependencies items 11-17):
  #   - E13 depends on E20-S5 (Railway pulls images from GHCR)
  #   - E20-S4 depends on E20-S3 (footer links to /about)
  #   - E18-S3 depends on E11-S2 (BETA banner needs ASPNETCORE_ENVIRONMENT=Beta)
  #   - E8 (when it resumes) depends on E14-S4 (rate-limiting baseline)
  #
  # Beta-pivot epics are ordered below by approximate first-wave-appearance.
  # E20 first because Wave 1 is here. E11..E18 in their first-wave order.
  # E19 last because it is Production prep and not on the Beta-Go-Live path.

  epic-20: done
  e20-s1-add-license-dco-and-contributing: done
  e20-s2-add-spdx-headers-policy-for-new-files: done
  e20-s3-add-backend-about-endpoint: done
  e20-s4-add-frontend-license-footer: done
  e20-s5-add-ghcr-image-publishing-pipeline: done
  epic-20-retrospective: done

  epic-11: done
  e11-s1-add-env-examples-and-document-config-precedence: done
  e11-s2-introduce-aspnetcore-environment-beta: done
  e11-s3-make-next-config-environment-driven: done
  epic-11-retrospective: done

  epic-12: done
  e12-s1-add-backend-dockerfile-multistage: done
  e12-s2-add-frontend-dockerfile-standalone: done
  e12-s3-add-custom-keycloak-image-with-spi: done
  e12-s4-add-optional-docker-compose-full: done
  epic-12-retrospective: done

  epic-13: done
  e13-s1-create-railway-project-and-services: done
  e13-s2-configure-railway-environment-variables: done
  e13-s3-enforce-public-and-private-networking: done
  e13-s4-add-health-probes-and-first-deploy: done
  epic-13-retrospective: done

  epic-15: done
  e15-s1-verify-two-postgres-separation: done
  e15-s2-add-database-automigrate-toggle: done
  e15-s3-add-daily-postgres-backup-to-rustfs: done
  e15-s4-document-beta-seeding-strategy: done
  epic-15-retrospective: done

  epic-16: backlog
  e16-s1-verify-frontend-public-urls: backlog
  e16-s2-validate-end-to-end-oidc-in-beta: backlog
  e16-s3-validate-document-upload-against-rustfs: backlog
  epic-16-retrospective: optional

  epic-14: backlog
  e14-s1-secrets-audit-and-repo-cleanup: backlog
  e14-s2-review-security-headers-and-https: backlog
  e14-s3-verify-hangfire-dashboard-dev-only: backlog
  e14-s4-add-rate-limiting-baseline: backlog
  e14-s5-audit-logs-for-sensitive-data: backlog
  epic-14-retrospective: optional

  epic-17: backlog
  e17-s1-restrict-serilog-to-console-in-containers: backlog
  e17-s2-validate-structured-logs-with-correlation-id: backlog
  e17-s3-add-frontend-api-health-endpoint: done  # absorbed-by-e13-s4 (DEC-1=A 2026-06-01)
  e17-s4-add-external-uptime-monitoring: backlog
  epic-17-retrospective: optional

  epic-18: backlog
  e18-s1-author-runbook-beta: backlog
  e18-s2-author-beta-tester-onboarding-guide: backlog
  e18-s3-add-beta-banner-in-ui: backlog
  e18-s4-add-feedback-channel: backlog
  epic-18-retrospective: optional

  epic-19: backlog
  e19-s1-add-custom-domain-runbook-entry: backlog
  e19-s2-perform-backup-restore-drill: backlog
  e19-s3-define-production-gate-checklist: backlog
  e19-s4-document-postal-smtp-migration-plan: backlog
  epic-19-retrospective: optional

  # === DEFERRED backlog — pre-Beta-pivot work (resumes after Beta validation) ===
  # Epics 4-8 were consciously reset to 'backlog' on 2026-05-14 because the
  # generic-platform initiative (E9 + E10) preempted them. SCP-2026-05-15
  # extended this hold: E11-E20 (Beta + OSS) now preempt E4-E8 as well.
  # Story files for many E4-E8 stories already exist on disk from the pre-pivot
  # planning pass, but they are intentionally HELD at backlog and NOT
  # auto-upgraded to ready-for-dev — the pivot decisions outrank file-presence
  # detection. When E4-E8 resume, E10 must already be done (it is) so the
  # external API route group (E8) is covered by module enforcement from day
  # one, and E14-S4 (rate-limiting baseline) must already be done so external
  # API routes inherit the rate-limit policy.
  epic-4: backlog
  e4-s1-add-event-fee-configuration: backlog
  e4-s2-connect-paid-registration-to-finance: backlog
  e4-s3-add-paid-registration-ui: backlog
  epic-4-retrospective: optional

  epic-5: backlog
  e5-s1-add-automation-definition-model-and-api: backlog
  e5-s2-add-automation-execution-engine: backlog
  e5-s3-add-automation-management-ui: backlog
  e5-s4-add-multi-channel-messaging-abstraction: backlog
  e5-s5-add-user-channel-preferences: backlog
  epic-5-retrospective: optional

  epic-6: backlog
  e6-s1-add-cost-center-and-budget-model: backlog
  e6-s2-associate-finance-records-with-cost-centers: backlog
  e6-s3-add-budget-vs-actual-reports: backlog
  epic-6-retrospective: optional

  epic-7: backlog
  e7-s1-define-accessibility-baseline-and-audit-critical-pages: backlog
  e7-s2-improve-shared-component-accessibility: backlog
  e7-s3-add-hindi-translation-expansion-path: backlog
  e7-s4-add-content-language-metadata-where-needed: backlog
  epic-7-retrospective: optional

  epic-8: backlog
  e8-s1-add-api-credentials-and-scopes: backlog
  e8-s2-add-read-api-endpoints: backlog
  e8-s3-add-webhook-subscriptions-and-signing: backlog
  e8-s4-add-webhook-delivery-retry-and-history: backlog
  epic-8-retrospective: optional
