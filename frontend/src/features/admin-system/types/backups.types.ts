// Backups resource types (E27-S4). Re-exported from the WRAPped transport
// (`backup`) so the slice stays decoupled from the raw lib path.
export type { BackupDto, BackupScheduleDto } from "../api/backup";
