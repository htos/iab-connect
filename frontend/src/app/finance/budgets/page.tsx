import { BudgetsContent } from "@/features/finance/components/budgeting/budgets-content";

/**
 * Budgets route (E26-S4). Thin server entry → the slice composition root.
 */
export default function BudgetsPage() {
  return <BudgetsContent />;
}
