import { SettingsActivityAreasContent } from "@/features/finance/components/settings/settings-activity-areas-content";

/**
 * Settings Activity Areas route (E26-S6). Thin server entry → the slice composition root.
 */
export default function ActivityAreasPage() {
  return <SettingsActivityAreasContent />;
}
