import { TaxCodesContent } from "@/features/finance/components/settings/tax-codes-content";

/**
 * Tax Codes route (E26-S6). Thin server entry → the slice composition root.
 */
export default function TaxCodesPage() {
  return <TaxCodesContent />;
}
