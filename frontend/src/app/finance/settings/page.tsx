import { SettingsHubContent } from "@/features/finance/components/settings/settings-hub-content";

/**
 * Finance Settings Hub route (E26-S6). Thin server entry → the slice composition root.
 */
export default function FinanceSettingsPage() {
  return <SettingsHubContent />;
}
