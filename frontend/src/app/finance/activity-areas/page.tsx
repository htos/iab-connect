import { ActivityAreasContent } from "@/features/finance/components/budgeting/activity-areas-content";

/**
 * Activity Areas route (E26-S4). Thin server entry → the slice composition root.
 */
export default function ActivityAreasPage() {
  return <ActivityAreasContent />;
}
