import { CategoriesContent } from "@/features/finance/components/budgeting/categories-content";

/**
 * Finance Categories route (E26-S4). Thin server entry → the slice composition root.
 */
export default function CategoriesPage() {
  return <CategoriesContent />;
}
