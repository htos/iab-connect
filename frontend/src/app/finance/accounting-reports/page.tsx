import { AccountingReportsContent } from "@/features/finance/components/accounting-reports-content";

/**
 * Accounting Reports route (E26-S2). Thin server entry → the slice composition root.
 */
export default function AccountingReportsPage() {
  return <AccountingReportsContent />;
}
