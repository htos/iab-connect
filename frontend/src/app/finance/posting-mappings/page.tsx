import { PostingMappingsContent } from "@/features/finance/components/posting-mappings-content";

/**
 * Posting Mappings route (E26-S2). Thin server entry → the slice composition root.
 */
export default function PostingMappingsPage() {
  return <PostingMappingsContent />;
}
