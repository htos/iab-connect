import { AccountsContent } from "@/features/finance/components/accounts-content";

/**
 * Finance Accounts route (E26-S2). Thin server entry → the slice composition root.
 */
export default function AccountsPage() {
  return <AccountsContent />;
}
